# 모더니아 동작 시나리오

> 모드: 보강 (파싱 후 · `data/parsed_skills.json` 기준. 초안의 유저 확인 사항은 그대로 보존)

## 기본 정보
- 코드: 작열 (우월 대상 = 풍압 적) / 클래스: 화력형 / 기업: 필그림 / 스쿼드: 헬레틱
- 버스트 단계: **B3**
- 본인 쿨다운: **40s** → **작성 사이클 수: 2**
- 무기 유형: **MG** (최대 장탄 300, 재장전 2.30s, 무기계수 7.71%, 코어 200%)
  - 예열: 1/s → 70/s 표기(실효 60/s, 프레임 상한). 41.4발에서 최대 → `context/scenarios/MG 예열.md`
- 스킬 값은 전부 **스킬레벨 10** 기준

## 효과 요약 (보강 — `parsed_skills.json` 기준, 값은 스킬레벨 10)

| ID (name) | 출처 | 트리거 | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|
| 고속진화 | 스킬1 | `hit_count:1` | `bonus_damage: 3.05` (최종 공격력 %) | `target` | — |
| 고속진화 2 | 스킬1 | `hit_count:200` | `crit_dmg: 14.25` · `max_stack: 5` | self | 10.0s |
| 고속진화 3 | 스킬1 | `hit_count:200` | `max_ammo_pct: -5.04` (`fixed_value`) · `max_stack: 5` · harmful | self | 10.0s |
| 대도약 | 스킬2 | `full_burst_start` | `accuracy_pct: 8.56` | `all_allies` | 15.0s |
| 대도약 2 | 스킬2 | `hit_count:200` + **`self_stat_above:accuracy_pct:0`** | `atk_pct: 29.38` | self | 10.0s |
| 신세계 | 스킬3 | `burst_cast` | `fullburst_duration: +5.0` (`fixed_value`) | `all_allies` | `null` |
| 섬멸 모드 | 스킬3 | `burst_cast` | `weapon_change` — MG · `damage_coeff 2.24` · `max_ammo: -1` · `core_dmg_mult 200` | self | 15.0s |

**구현 상태**: `self_stat_above:accuracy_pct:0` 1종만 ❌ → 단계 4 대상. 나머지는 전부 ✅
(`accuracy_pct`는 ⚠️ = DPS 미사용이지만 `get_buffs()` 집계는 되므로 위 condition의 판정 재료로 충분).
`섬멸 모드`는 벨벳 `깔끔한 마무리`(MG · `max_ammo: -1` · `duration`)와 형태가 완전히 같아 검증된 경로다.

**딜 구조 (표준 스쿼드 · seed=1 · 60초 실측)**: 모더니아 94,224,654 (스쿼드의 30.6%) —
기본공격 54,643,672(58.0%, 3270회) / `고속진화` 39,580,982(**42.0%**, 3269회).
스킬 딜이 전부 `고속진화` 하나다. 버스트(신세계·섬멸 모드)는 **직접 딜이 0**이고
무기계수를 오히려 낮춘다 — 딜은 전부 매 히트 추가딜과 풀버스트 연장으로 돌아온다.

**딜 구조 요약**: 본인 딜의 대부분은 *버스트가 아니라* 고속진화 ①(매 히트 추가딜) × MG 60발/s에서 나온다.
버스트(신세계)는 딜 계수를 오히려 **낮추는 대신**(7.71% → 2.24%) 장탄 무한 + 풀버스트 5초 연장을 준다 —
다수 적에서 이득, **단일 보스 sim에서는 손해일 가능성**이 크다. 이 점이 이번 등록의 핵심 관전 포인트.

## 검증 스쿼드

- **표준**: `["리틀 머메이드", "크라운", "모더니아", "test_B3"]`
  - B3·쿨 40s 표준 템플릿. 모더니아를 B3 맨 앞에 두어 1사이클부터 버스트 발동
- **변형 1 (실전 작열 조합 · 적 코드 풍압)**: `["리타", "그레이브", "앨리스", "레이", "모더니아"]`
  - `HARNESS.md` `레이드_작열짬`(현재 4인)에 모더니아를 맨 뒤로 넣은 실전 운용
  - B3가 3명(앨리스·레이·모더니아)이라 모더니아 버스트는 **늦은 사이클에만** 발동 →
    "버스트를 거의 못 쓰는 모더니아"의 딜이 실전 기준값
  - 적 코드 **풍압**으로 돌려 작열 우월(⑦)이 걸린 상태를 본다
- **변형 2 (버스트 미사용 — 실전 권장 운용)**: 변형 1과 같은 멤버 +
  `config={"no_burst_char": "모더니아"}`
  - **유저 확인: 실전에서 모더니아는 버스트를 쓰지 않는 것이 권장**이다(아래 §유저 확인 사항).
    변형 1과의 딜 차이가 그 판단을 수치로 재현하는지 확인한다
  - 기대: 변형 2가 **더 높거나 비슷**해야 정상. 변형 1이 크게 앞서면 섬멸 모드 계수 하락이나
    버스트 간격 증가가 시뮬에 제대로 반영되지 않은 것이다

**표준 스쿼드는 적 코드 `없음` / `풍압` 양쪽으로 돌린다** — 모더니아 자신은 코드 조건 효과가 없으나,
⑦ 경로가 붙었을 때 매 히트 추가딜(고속진화 ①)의 비중이 어떻게 변하는지 확인.

## 사이클 타임라인 (표준 스쿼드 · seed=1 · **실측**)

| t | 이벤트 | 발동 효과 | 대상/지속/스택 | 비고 |
|---|---|---|---|---|
| 0.0 | battle_start | — | | 모더니아는 `battle_start` 효과 없음. 사격 시작(예열 0) |
| ~2.9 | 예열 최대 도달(41.4발) | — | | 이후 60발/s(프레임 상한) |
| 3.000 / 3.100 | stage:1 리틀 머메이드 / stage:2 크라운 | — | | |
| **3.200** | **stage:3 모더니아** | `신세계`(fullburst_duration +5) · `섬멸 모드`(weapon_change) | 신세계=아군 전체 / 모드=self 15s | 계수 7.71 → 2.24 |
| 3.250 | full_burst 시작 | `대도약` accuracy_pct 8.56 | 아군 전체 / 18.25까지 | 풀버스트 = 10+5 = **15초** ✔ |
| ~5.5~ | 일반 공격 200발마다 | `고속진화 2`·`고속진화 3`·`대도약 2` | self / 10s, 최대 5스택 | 무한 장탄 구간이라 재장전 공백 없이 ~3.33s 간격 |
| **18.250** | full_burst 종료 = 섬멸 모드 종료 | 무기 원복(2.24 → 7.71) | | 15초가 정확히 겹친다 |
| 20.250~20.450 | 2사이클 B1→B2→**B3 test_B3** | `대도약` 재발동 | 아군 전체 / 15s | `신세계`·`섬멸 모드` **미발동**(`burst_cast` 네거티브) ✔ |
| 20.500 | full_burst 시작 | | | 풀버스트 **10초**(연장 없음) → 30.500 종료 ✔ |
| 32.983 | 3사이클 **stage:3 모더니아** | 신세계·섬멸 모드 재발동 | | 모더니아 버스트 주기 = **29.78s** |
| 33.033 | full_burst 시작 | | | 다시 15초 → 48.033 종료 |

**사이클 간격**: `3.25 → 20.50 → 33.03 → 50.28` = **17.25 / 12.53 / 17.25** 교대.
초안 Q7의 우려(풀버스트 15초가 다음 사이클을 밀어낸다)가 **그대로 실측됐다** — 모더니아가 버스트를
쓴 사이클은 풀버스트가 5초 길어진 만큼 다음 진입이 밀려 간격이 12.53 → **17.25초**가 된다.
이것이 유저가 말한 "버스트 간격이 늘어난다"의 정체다. 버그 아님.

**60초 실측 집계 (표준 · seed=1 · 적 코드 없음)**: 모더니아 94,224,654 (스쿼드의 30.6%) —
기본공격 3,270회 58.0% / `고속진화` 3,269회 42.0%.
구간별: 본인 풀버스트 47.5% / 타인 풀버스트 42.5% / 비풀버스트 10.0%.

### 변형 1 예상 차이
- 1사이클 B3는 앨리스 → 모더니아는 **대도약 ①만** 수령, 신세계 미발동
- 모더니아 버스트는 3명 교대라 60초 내 1~2회. 리타 버쿨감 프로필로 사이클 간격 `17.67 → 14.97 → 12.25`
- 작열 코드 아군(그레이브·앨리스·레이·모더니아)이 레이의 코드 필터 버프를 수령 → 모더니아 딜이 크게 뜬다

## 스킬 간 상호작용

- **대도약 ①(명중률) → 대도약 ②(공격력) 게이트**
  - 경로: 풀버스트 시작 시 명중률 8.56% ▲(15s) → 이후 200발째에 "명중률 증가 상태" 판정 통과 → 공격력 29.38% ▲(10s)
  - 시점: **전투 시작 ~ 첫 풀버스트(t≈3.25) 이전 구간에는 명중률 버프가 없다** → 이 구간의 200발째는 대도약 ②가 무발동이어야 정상
  - 위험: 조건을 "항상 참"으로 뭉개면 초반 구간이 과대평가된다. 네거티브 검증 항목
  - 참고: `hit_count` 카운터는 condition 충족과 무관하게 계속 진행(GAMEPLAY §condition) → 조건 불충족 구간의 200발도 위상을 민다

- **신세계 ③(섬멸 모드) → 고속진화 ①(매 히트 추가딜)**
  - 경로: 무기 변경으로 무기계수는 7.71→2.24로 떨어지지만, 고속진화 ①의 추가딜(최종 공격력 3.05%)은
    **무기계수와 무관**하게 히트당 붙는다 → 모드 중 딜 비중이 추가딜 쪽으로 크게 기운다
  - 시점: 유저 확인대로 섬멸 모드 사격도 일반 공격 판정 → 추가딜·카운터 모두 정상
  - 위험: 무기 변경 시 추가딜이 끊기게 구현하면 버스트 구간 딜이 붕괴한다. 검증 항목

- **신세계 ②(무한 장탄) → 예열 유지 → 200발 카운터 가속**
  - 경로: 재장전 없음 → 예열 냉각 없음 → 60발/s 고정 → 200발 트리거가 3.33s 간격으로 최대 밀도
  - 곁가지: 고속진화 ②의 디메리트(최대 장탄 5.04%▼ ×5 = −25.2%)는 **무한 장탄 구간에서 무효**,
    모드 종료 후에만 재장전 빈도를 늘린다 → 디메리트가 버스트 밖으로 밀리는 구조

- **신세계 ①(풀버스트 +5초) → 스쿼드 전원**
  - 모더니아 본인 딜이 아니라 **팀 딜**로 회수되는 효과. 표준 스쿼드에서는 리틀 머메이드·크라운·test_B3가 수혜
  - `fullburst_duration`은 `burst_cast` 등록 시 **본인이 그 사이클의 B3 발동자일 때만** 반영(IMPL-STATUS) → 정상 동작

- **순환 없음**: 고속진화 ②·대도약 ②가 서로를 트리거하지 않고, 명중률 → 공격력의 단방향

## 유저 확인 사항

- Q: 섬멸 모드의 무기 스펙(무기유형·연사·코어 대미지)이 텍스트에 없다. 어떻게 잡나?
  - A: **MG 그대로, 예열 유지.** `weapon_type: MG`, 연사·예열 곡선·코어 200% 모두 원래 무기와 동일하게 두고
    `damage_coeff`만 7.71 → 2.24로 교체한다. (조준선만 넓어진 같은 총이라는 해석)
- Q: 섬멸 모드 중의 사격이 "일반 공격"으로 판정되는가? (고속진화 ① 추가딜, 고속진화 ②·대도약 ②의 200회 카운터)
  - A: **전부 판정된다.** 무기 변경 모드의 사격도 일반 공격 트리거를 정상 발동시킨다는 기존 규칙
    (`GAMEPLAY.md §무기 메카닉` — 맥스웰 : 오디너리 미케닉 사례)과 일치. 단 "예상"이라는 단서가 붙었으므로
    구현 후 인게임 대조 대상.
- Q: 대도약 ②의 "자신이 명중률 증가 상태라면" 조건 판정 기준은?
  - A: **명중률 stat이 0보다 크면 참.** 누가 준 명중률이든(본인 대도약 ① 포함) `accuracy_pct` 합이
    양수면 조건 충족. → 기존 `self_state:상태명`(이름 기반)으로는 표현 불가 → **신규 condition 키 필요**.
- Q: 검증 스쿼드는?
  - A: **표준 + 실전 작열 조합** 둘 다. (위 §검증 스쿼드)

- Q: 신세계 ②`[장탄 수 무한] [15초 유지]`를 별도 stat으로 둘 것인가?
  - A: **섬멸 모드 무기 변경의 `max_ammo: -1`로 통합한다.** 별도 `infinite_ammo` stat을 만들지 않는다
    (IMPL-STATUS ❌ 미구현이고, `PARSING.md §7-13`이 무기 변경의 장탄 무한을 `-1`로 적도록 규정).
    → 부작용을 명시해 둔다: 통합하면 무한 장탄이 **섬멸 모드에 종속**된다. 둘 다 15초라 현재는 동일하지만,
    모드가 먼저 풀리는 경로가 생기면 무한 장탄도 함께 끝난다.
- Q: 고속진화 ②의 `최대 장탄 수 5.04% ▼`가 스킬레벨 무관 고정값이다(values에 없음). 어떻게 적나?
  - A: **`fixed_value`로 처리.**
- Q: 섬멸 모드의 "조준 범위 내 적 전체 동시 조준 / 보스는 단일 적 간주"를 단일 보스 sim에서 어떻게 다루나?
  - A: **1히트로 수렴.** 다중 조준은 딜 배수가 되지 않는다
    (GAMEPLAY §condition `적용 대상 N ▲` 클래스와 동일 취급).
- Q: 실전에서 모더니아 버스트 운용은?
  - A: **버스트를 쓰지 않는 것이 권장.** ① 섬멸 모드로 무기계수가 7.71% → 2.24%로 떨어져 본인 딜이 약해지고,
    ② B3 슬롯을 점유해 **버스트 간격이 늘어난다**. 테스트에서는 버스트 스킬 검증을 위해 쓰는 조합도 돌리되,
    **버스트 미사용 조합(변형 2)이 실전 기준선**이다.

### 초안의 남은 의문점 — 해소 결과

- **Q7 (풀버스트 15초가 다음 사이클과 겹침) → 해소.** 겹치지 않는다. 다음 사이클 진입이
  풀버스트 종료 뒤로 **밀린다**: 사이클 간격이 `17.25 / 12.53 / 17.25`로 교대한다.
  모더니아가 버스트를 쓴 사이클만 +4.7초 늘어난다 → 유저가 말한 "버스트 간격이 늘어난다"가
  정확히 이 현상이다. 정상 동작.
- **Q8 (200회 카운터 공유) → 해소.** `hit_count:N`은 caster별 단일 카운터라 같은 200발째에
  `고속진화 2`·`고속진화 3`·`대도약 2`가 **동시 발동**한다(실측 만료시각 3종 모두 동일).
  실행 순서는 서로를 읽지 않아 결과 불변. 배열 순서는 원문대로(고속진화 → 대도약) 유지.

## 해석 선언

| 효과 | 해석 | 근거 | 대안 해석이었다면 |
|---|---|---|---|
| `고속진화` | stat `bonus_damage`, `hit_count:1` — **매 히트** 발동 | "일반 공격 명중 시 [최종 공격력 3.05% 추가 대미지]". 팬텀 `괴도의 시선`과 같은 형태 | 200발 트리거에 묶였다면 모더니아 딜의 42%가 통째로 사라진다 |
| `고속진화` 계산 시점 | `burst_cast` 보류(`_pending_burst_dmg`) 대상이 **아니다** — 매 히트 즉시 계산 | 보류는 `timing: burst_cast`인 `bonus_damage` 전용. 이쪽은 `hit_count:1` | 해당 없음 |
| `섬멸 모드` | `weapon_change` MG · `damage_coeff` 2.24 — 무기계수를 **낮춘다** | "대미지 : 최종 공격력 2.24% 대미지". 원 무기 7.71% | 별도 추가딜이었다면 버스트가 딜 상승이 된다. 실제로는 **하락**이고, 그게 "버스트 안 쓰는 게 권장"의 근거 ① |
| `섬멸 모드` 무기 스펙 | MG 기본값 승계(1/s → 70/s, 예열 41.4발, 코어 200%) | 유저 확인 "MG 그대로, 예열 유지". `weapon_mechanics.json` MG 기본값이 모더니아 자기 무기 수치와 **동일**해 별도 필드 불필요 | 예열이 리셋되는 해석이었다면 모드 진입 직후 연사가 1/s에서 다시 올라 버스트 구간 딜이 더 떨어진다 |
| `섬멸 모드` 예열 연속성 | 모드 진입·종료에 `warmup_shots`가 보존된다 | `_tick_weapon_change`가 `warmup_bullets`만 교체·복원하고 `warmup_shots`는 건드리지 않는다 — 코드 기본 동작이 유저 답과 일치 | 리셋이었다면 코드 수정이 필요했다 |
| 장탄 무한 | `섬멸 모드`의 `max_ammo: -1`로 통합, 별도 항목 없음 | 유저 확인 + `PARSING.md §7-13` | 별도 `infinite_ammo` buff였다면 ❌ 미구현이라 **딜 기여 0** — 모드 중 재장전이 발생해 예열이 식었을 것 |
| 모드 종료 시 장탄 | **풀 장탄으로 복원** (모드 종료 = 재장전 완료 상태) | 유저 확인. 구현 전에는 복원 자체가 없어 무한 장탄이 원래 무기로 새어 나갔다 | 진입 직전 장탄 복원이었다면 종료 직후 재장전이 더 빨리 오고, 0 복원이었다면 종료 즉시 재장전 2.3초가 들어가 딜이 더 낮아진다 |
| `신세계` | `fullburst_duration` `fixed_value: 5.0`, `duration: null` | 이사벨 `소닉 체이서 5`·소다 `시간 연장 I/II` 선례와 동형 | `duration`을 넣으면 `_fb_caster` 판정 경로가 달라진다 |
| `대도약 2` 조건 | `self_stat_above:accuracy_pct:0` — **stat 값** 기준 | 유저 확인 "명중률 stat이 0보다 크면 참". `self_state:대도약`(이름 기준)이 아님 | 이름 기준이었다면 다른 캐릭터가 준 명중률로는 조건이 안 켜진다 |
| `고속진화 3` | `max_ammo_pct: -5.04` harmful, 5중첩 = **−25.2%** | "최대 장탄 수 5.04% ▼ [5중첩]" | 부호를 뒤집으면 디메리트가 버프가 된다. **섬멸 모드 중에는 무한 장탄이라 무효**, 모드 밖에서만 재장전 빈도를 올린다 |
| 다중 조준 | 단일 보스에서 **1히트** | "보스는 저지 부위 및 파츠 무관, 단일 적으로 간주" + 유저 확인 | 다수 적 모델이 생기면 섬멸 모드의 평가가 뒤집힌다 |

## impl/bug-fix용 검증 체크리스트

**단계 4 구현 대상은 `self_stat_above:accuracy_pct:0` 1건뿐이다.** 나머지는 회귀 확인 항목.

- [x] **`대도약 2`는 명중률 버프가 없는 구간의 200발째에 무발동** (네거티브 — 구현 항목)
      - 재현: `python -m context.sim "리틀 머메이드,크라운,모더니아,test_B3" --seed 1 --first-burst 12 --char 모더니아 --view buff`
      - 수정 전: t=12.250 스냅샷에 `대도약 2`가 이미 활성(15.80까지) — `_condition_ok`가
        모르는 condition을 `True`로 흘려보냈다. 수정 후 **사라졌고**, 명중률이 붙은 뒤인
        t=29.500에는 정상 발동한다. 같은 트리거의 `고속진화 2`·`3`(조건 없음)은 양쪽 다 발동
      - 표준 스쿼드(`--first-burst` 미지정)에서는 첫 풀버스트(3.25s)가 200발(≈5.5s)보다 빨라
        **차이가 안 난다** — 이 회귀는 `--first-burst 12`로만 잡힌다
- [x] 모더니아 버스트 시전 시 무기 계수 7.71 → 2.24 교체, 15초 후 원복 — t=3.200 진입 / 18.250 종료 확인
- [x] 풀버스트 지속: 모더니아 발동 사이클 **15초**(3.25→18.25, 33.03→48.03) / 미발동 사이클 **10초**(20.50→30.50)
- [x] `섬멸 모드` 중 200회 카운터 계속 진행 → `고속진화 2`·`3`·`대도약 2` 정상 발동 (t=20.5 스냅샷 3종 동시 활성)
- [x] `신세계`·`섬멸 모드`는 본인 미발동 사이클에 **발동 금지** — 2사이클(test_B3) 미발동 확인
- [x] `대도약`은 본인 미발동 사이클에도 발동 (`full_burst_start`) — 2사이클 35.50까지 활성 확인
- [x] 사이클 간격 `17.25 / 12.53 / 17.25` 교대 — 버스트 쓴 사이클만 +4.7초 (Q7 해소)
- [x] `섬멸 모드` 중 재장전 **0회**, 60.0발/s 유지 — 3.25~18.25 구간 900발 전부 1프레임 간격.
      긴 공백은 cold start(t=1.0/1.38/1.62)와 모드 밖 재장전(24.67/29.67)뿐
- [x] **모드 종료 시 장탄이 풀로 복원된다** — t=18.5에 ammo 999,111 → **250**
      (300 × (1 − 5.04%×2스택)). 수정 전에는 센티널 999,999가 원래 무기로 새어 나가
      모드가 끝난 뒤에도 재장전이 사라졌다 → `timeline.py` `_wc_ammo_borrowed` 추가로 해결.
      **모더니아 고유 버그가 아니라 기존 엔진 결함**이었다(목단 `정정당당 승부다!`도 해당)
- [x] `고속진화 3`의 장탄 감소가 모드 종료 후 재장전 빈도에 반영 — 복원값이 300이 아니라
      스택 반영된 250/241로 나온다
- [x] 버스트 구간 딜이 비버스트 구간보다 낮은 것이 **정상** — 히트당 기본공격 딜
      섬멸 모드 **9,965** ↔ 다음 사이클 풀버스트 **28,113** (약 1/3).
      반대로 `고속진화` 추가딜은 13,221 ↔ 11,108로 모드 중이 더 높다(무기계수와 무관하기 때문)
- [x] 회귀 **24/24 PASS** — `레이드_작열짬`에 모더니아 추가.
      baseline 변동은 `레이드_아니스서머메이든`(목단) 하나뿐이고, 원인은 모더니아가 아니라
      시한부 무한 장탄 모드 종료 시 장탄 누출 수정(커밋 `cef800b`)이다.
      ⚠️ 그 스쿼드에서 모더니아는 버스트를 안 쓰므로 **`신세계`·`섬멸 모드`는 하네스 미커버**다
      (유저 판단으로 버스트 전용 지그를 두지 않았다). 위 표준 스쿼드로 단발 확인한다
- [ ] ~~변형 2(버스트 미사용) ≥ 변형 1(버스트 사용)~~ — **측정 불가로 판명.**
      실전 조합에서 모더니아는 B3 셋 중 맨 뒤라 180초 내내 앨리스·레이가 교대하며
      **버스트가 한 번도 안 나간다** → `no_burst` 설정이 no-op이고 두 변형의 딜이 완전히 동일하다
      (1,530,597,869 / 모더니아 430,372,733). 유저의 "버스트 안 쓰는 게 권장"은 편성 단계에서
      이미 구조적으로 강제되고 있다. 버스트의 손익은 위 히트당 딜 비교로 대신 확인한다
