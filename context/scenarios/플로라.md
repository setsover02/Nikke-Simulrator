# 플로라 동작 시나리오

> 모드: 보강 (`/char-parse` 후 — `data/parsed_skills.json` 기준)

## 기본 정보
- 버스트 단계: B2
- 본인 쿨다운: 40s
- 무기 유형: MG (최대 장탄 300, 재장전 2.5s, 코어 200%, `damage_coeff` 5.57)
- 속성/클래스/기업: 전격 / 지원형 / 미실리스
- 작성 사이클 수: 2 (쿨 40s → 본인 버스트 미사용 사이클 존재)
- 애장품: **꽃말 노트** 보유. **0~3단계 전 판본 파싱 완료** (2026-08-17).
  교체슬롯 매핑: 1단계→스킬1, 2단계→스킬2, 3단계→스킬3.
  아래 효과 요약·타임라인·상호작용은 **3단계 기준**이며, 낮은 단계 차이는 `## 애장품 단계별 차이` 참조

## 효과 요약 (parsed_skills.json 10개, 값은 Lv10)

| ID | 출처 | 트리거 | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|
| 마음의 평화 | 스킬1 | `battle_start` | `heal_hp_pct` 1.0 (`tick_interval` 1s) | `allies_adjacent:2` | 지속(-1) |
| 피튜니아 | 스킬1 | `battle_start` | `heal_received_pct` 4.0, `max_stack` 5 | `allies_adjacent:2` | 지속(-1) |
| 피튜니아 2 | 스킬1 | `hit_count:100` | `buff_max_stack_add` +1 | `allies_code:전격` | 지속(-1) |
| **피튜니아 3** | 스킬1 | `burst_enter:2` | `hp_only_caster_based_pct` 15.01 | `allies_adjacent:2` | **2.0s** |
| 아이리스 | 스킬2 | `event:adjacent_hp_below:90` | `shield_from_max_hp_pct` 10.22 | `all_allies` | 10.0s |
| **아이리스 2** | 스킬2 | `event:adjacent_hp_max` | **`armor_break_dmg_pct` 30.97** | `all_allies` | 10.0s |
| 아이리스 3 | 스킬2 | `event:shield_applied` | `atk_caster_based_pct` 45.12 | `allies_adjacent:2` | 10.0s |
| 시크릿 가든 | 스킬3 | `burst_cast` | `heal_hp_pct` 10.45 | `all_allies` | (instant) |
| **시크릿 가든 2** | 스킬3 | `burst_cast` | **`armor_break_dmg_pct` 42.39** | `all_allies` | 10.0s |
| **시크릿 가든 3** | 스킬3 | `burst_cast` | **`atk_caster_based_pct` 85.86** | `all_allies` | 10.0s |

딜 기여는 굵은 4개와 아이리스 3이다. 나머지는 힐·보호막·스택한도로 DPS에 직접 영향이 없다.

### 구현 상태 (IMPL-STATUS 대조)

| 키 | 상태 | 영향 |
|---|---|---|
| `armor_break_dmg_pct`·`atk_caster_based_pct`·`hp_only_caster_based_pct`·`heal_hp_pct` | ✅ | — |
| `allies_adjacent:2`·`allies_code:전격`·`all_allies`·`hit_count:100`·`burst_enter:2`·`burst_cast` | ✅ | — |
| **`event:adjacent_hp_max`** | ✅ | 피튜니아 3 만료 후 100% 복귀 전이를 감지해 아이리스 2 발동 |
| `event:adjacent_hp_below:90` / `shield_from_max_hp_pct` / `event:shield_applied` | ✅ / ✅ / ✅ | 피튜니아 3의 체력 비율 하락 → 아이리스 보호막 → 아이리스 3 연계 |
| `heal_received_pct` / `buff_max_stack_add` | ❌ / ❌ 신규 | DPS 무영향 |

## 검증 스쿼드

- **표준**: `["리틀 머메이드", "플로라", "크라운", "test_B3", "스노우 화이트 : 헤비암즈"]`
  — B2 40s 템플릿. 플로라를 B2 후보 맨 앞에 둬 본인 버스트 검증.
  이 배치에서 `allies_adjacent:2` = **플로라 + 리틀 머메이드 + 크라운**.
- **변형 1 (인접 대상 확인)**: `["리틀 머메이드", "크라운", "플로라", "test_B3", "스노우 화이트 : 헤비암즈"]`
  — 플로라를 3번 슬롯으로 옮겨 인접 아군이 크라운·test_B3로 바뀌는지 확인.
  단 B2 후보에서 크라운보다 뒤로 밀리므로 **본인 버스트 미발동** — 아이리스 2 경로 전용 검증.
- (선택) **변형 2 (전격 딜러 포함)**: `buff_max_stack_add` 구현 시에만 의미. 현재 미구현이라 생략.
- **변형 3 (방무 딜러 포함)**: `["리틀 머메이드", "플로라", "크라운", "치사토", "test_B3"]`
  — `armor_break_dmg_pct`(아이리스 2 · 시크릿 가든 2)는 `is_armor_break_damage` 히트에만 실린다.
  표준 스쿼드에는 `armor_break_enabled` 공급원이 없어 두 버프가 딜에 전혀 안 나타나므로,
  **애장품 단계별 딜 차이를 보려면 이 스쿼드가 필요하다** (2026-08-17 추가).

## 사이클 타임라인 (표준 스쿼드, 2사이클)

| t (예상) | 이벤트 | 발동 효과 | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0.0 | `battle_start` | 마음의 평화(HoT 1%/1s), 피튜니아(`heal_received_pct` 1중첩~5) | 플로라+리머+크라운 / 지속 | DPS 무영향 |
| 0.0~ | MG 사격 개시 | `hit_count` 누적 | — | 100회마다 피튜니아 2 |
| ~10.0 | B1 리틀 머메이드 | — | | |
| ~10.1 | **B2 진입** (플로라) | **피튜니아 3** `hp_only_caster_based_pct` +15.01 | 플로라+리머+크라운 / 2s | 최대치만 ↑ → **hp_pct 100 → 약 86.9%** |
| ~10.1 | 같은 프레임 `burst_cast` | 시크릿 가든(회복 10.45%) / **시크릿 가든 2** def_ignore 42.39 / **시크릿 가든 3** atk_caster 85.86 | `all_allies` / 10s | 회복은 hp_pct를 96%까지만 올린다(회복 10.45% < 최대치 증가 15.01%) → 만피 복귀 실패 |
| ~10.1 | (hp_pct 86.9 ≤ 90) | 아이리스(보호막) → 아이리스 3(atk 45.12) | `all_allies` / 10s | 보호막을 받은 플로라에게 `event:shield_applied` → 플로라+리머+크라운에 공격력 증가 |
| ~10.2 | 풀버스트 진입 | 플로라 고유 `full_burst_start` 효과 없음 | — | |
| **~12.1** | **피튜니아 3 만료 → 최대치 원복 → hp_pct 100% 재도달** | **아이리스 2** `armor_break_dmg_pct` 30.97 | `all_allies` / 10s | 실측상 항상 이 경로. 추가 힐러가 있으면 ~10.1로 앞당겨질 수 있음 |
| ~20.1 | 시크릿 가든 2·3 만료 | def_ignore 42.39 + atk 85.86 해제 | | |
| ~22.1 | 아이리스 2 만료 | def_ignore 30.97 해제 | | |
| ~22.5 | 1사이클 종료 | | | |
| ~35 | 2사이클 **B2 진입 = 크라운** (플로라 쿨 40s 미회복) | 피튜니아 3 재발동(`burst_enter:2`는 누가 눌러도 발동) → 2s 후 **아이리스 2 재발동** | `all_allies` / 10s | **본인 버스트 미사용 사이클에도 아이리스 2는 돈다.** 시크릿 가든 3종은 무발동 |
| ~50 | 3사이클 | 플로라 버스트 재사용 | | 시크릿 가든은 **2사이클당 1회** |

## 스킬 간 상호작용

- **피튜니아 3 → 아이리스 2 (핵심 루프)**
  - 경로: `burst_enter:2` → `hp_only_caster_based_pct` +15.01(2s) → 인접 아군 hp_pct 100 → 약 86.9 →
    2초 뒤 만료로 최대치 원복 → **100% 재도달** → `event:adjacent_hp_max` → `armor_break_dmg_pct` 30.97 (10s)
  - 시점: B2 진입 +2.0s (또는 회복으로 더 일찍)
  - `max_hp_only_pct`·`hp_only_caster_based_pct`의 활성/만료 후 `sync_hp`에서 hp_pct 100%
    재도달 전이를 감지해 notify한다.
- **피튜니아 3 → 아이리스 → 아이리스 3**
  - 같은 86.9% 하락이 `event:adjacent_hp_below:90`도 만족 → 보호막 → `event:shield_applied` → atk 45.12
  - 보호막 적용 대상 중 플로라가 포함되므로 플로라 자신의 `event:shield_applied`가 발동한다.
  - 현재 아군 피격 모델이 없어 보호막은 10초 지속시간 동안 소모되지 않는다.
- **시크릿 가든(회복) ↔ 피튜니아 3 (동일 timing 실행 순서)** — *구현 후 실측으로 해소*
  - `burst_enter:2`와 `burst_cast`가 사실상 같은 프레임. 회복이 최대치 증가분을 즉시 메우면
    아이리스 2가 2초 앞당겨 발동한다
  - **실측(2026-08-02, 표준 스쿼드)**: 회복량(시전자 최대체력 10.45%)이 최대치 증가분(15.01%)보다 작아
    hp_pct가 86.95 → 96%까지만 오른다. **플로라 단독으로는 만피 복귀가 불가능**하고 항상
    피튜니아 3 만료(+2.0s) 경로로 발동한다
  - **단, 스쿼드에 추가 힐러가 있으면 2초 안에 만피가 채워져 앞당겨질 수 있다** (미검증 — 필요 시 힐러 포함 스쿼드로 확인)
  - 어느 쪽이든 아이리스 2의 발동 **시점만** 바뀌고 발동 여부는 바뀌지 않는다
- **아이리스 2 ↔ 시크릿 가든 2 중첩**
  - 둘 다 `armor_break_dmg_pct`, 서로 다른 name → **동시 활성 시 합산**(30.97 + 42.39). 갱신 아님

## 유저 확인 사항

- **Q1. 아이리스 발동 가능성** (아군 피해 모델 없음)
  - **A: 피해 모델은 없지만 「최대 체력만 증가」 개념은 있다. 피튜니아 3(2초)로 hp_pct가 100% 아래로
    내려가고, 2초 후 만료되면(또는 그 사이 힐로) 다시 100%에 도달한다 → 아이리스 2는 매 사이클 정상 발동.**
    보호막(아이리스·아이리스 3)은 보호막 적용 상태와 적용 이벤트까지 구현한다.
- **Q2. `[N 중첩]` 표기**
  - **A: 일반 표기법으로 "최대 N중첩"을 뜻한다. 1중첩에서 시작해 누적.** → `max_stack: 5`
- **Q3. 「일반 공격 100회 공격 시」**
  - **A: 일반적인 문법 — 100회마다 반복 발동.** → `hit_count:100`, 지속은 `-1`(영구)
- **Q4. 애장품 포함 여부**
  - **A(2026-08-02): 계산기는 애장품 3단계만 다룬다.** 애장품 3단계 템플릿이 정본, 단계 태그 필드 없음
  - **갱신(2026-08-17): 0~3단계 전부 지원한다.** 애장품 판본 항목에 `favorite`(교체 단계) 태그가
    붙고 기본 판본이 별도 슬롯으로 들어왔다. 3단계가 여전히 기본값이라 위 서술은 그대로 유효하다
- **Q5. 「자신의 양 옆 아군」**
  - **A: 기존 `allies_adjacent:N`으로 해결** (신규 구현 불필요). 「자신과 양 옆 아군 2기」 = `allies_adjacent:2`

## 애장품 단계별 차이

교체슬롯: 1단계→스킬1, 2단계→스킬2, 3단계→스킬3. 단계마다 슬롯 하나가 통째로 갈린다.

| 단계 | 갈리는 슬롯 | 3단계 대비 없는 것 | 효과 수 |
|---|---|---|---|
| 0 | 전 슬롯 기본 | `피튜니아 3`(hp_only_caster) · `아이리스 3` · `시크릿 가든 3` | 7 |
| 1 | 스킬1 애장품 | `아이리스 3` · `시크릿 가든 3` | 8 |
| 2 | +스킬2 애장품 | `시크릿 가든 3` | 9 |
| 3 | 전 슬롯 애장품 | — | 10 |

**스킬1의 이름이 판본 간 한 칸씩 밀린다** (`PARSING-CHARS.md §캐릭터별 예외`). 애장품 1단계의
`[마음의 평화 : ...]` 이름이 기본 판본에는 없어 무명 주기 회복이 스킬 키 이름 `피튜니아`를 가져간다:

| 실제 효과 | 기본 판본 이름 | 애장품 1단계 이름 |
|---|---|---|
| 주기 회복 (`heal_hp_pct`) | `피튜니아` | `마음의 평화` |
| 받는 회복량 (`heal_received_pct`) | `피튜니아 2` | `피튜니아` |
| 중첩 한도 (`buff_max_stack_add`) | `피튜니아 3` | `피튜니아 2` |
| 최대 체력 (`hp_only_caster_based_pct`) | **없음** | `피튜니아 3` |

같은 이름이 판본마다 다른 효과를 가리키지만 **판본은 공존하지 않고**, 스킬2의
`마음의 평화 상태인 아군 전체`는 이름이 아니라 `allies_adjacent:2`로 파싱돼 있어(대상 집합이 같다)
판본을 넘는 참조가 끊기지 않는다.

### 0·1단계에서 스킬2가 통째로 무발동한다

**핵심 루프의 유일한 발생원이 `피튜니아 3`(`hp_only_caster_based_pct`)이고, 이건 애장품 1단계에만 있다.**
계산기는 아군 피격을 모델링하지 않으므로 인접 아군의 hp_pct를 움직이는 것은 이 버프의
활성/만료 전이뿐이다(위 §스킬 간 상호작용 참조). 0단계에는 그 전이가 없다:

- `event:adjacent_hp_below:90` 미발생 → `아이리스`(보호막) 0회 → `event:shield_applied` 0회
- `event:adjacent_hp_max` 미발생 → **`아이리스 2`(`armor_break_dmg_pct` 30.97) 0회**

인게임에서는 보스 피격으로 아군 체력이 실제로 깎이므로 0단계에서도 돈다. **sim 한계이지 파싱
오류가 아니다** — 0단계 플로라의 실전 지원력은 계산기가 과소평가한다. 1단계 이상에서는
`피튜니아 3`이 붙어 3단계와 동일하게 동작한다(아래 실측이 0↔1단계 딜 동률인 이유).

### 실측 (표준 스쿼드, seed 1, 180s)

| 단계 | 플로라 딜 | 스쿼드 총딜 | 아이리스 / 아이리스 2 | 아이리스 3 | 시크릿 가든 3 |
|---|---|---|---|---|---|
| 0 | 180.5M | 2,171.3M | **0 / 0** | 0 | 0 |
| 1 | 180.5M | 2,171.3M | 220 / 210 | 0 | 0 |
| 2 | 237.8M | 2,421.3M | 220 / 210 | 153 | 0 |
| 3 | 288.3M | 2,742.8M | 220 / 210 | 153 | 75 |

0단계와 1단계의 딜이 **다섯 캐릭터 전부 자릿수까지 같다**. 스킬2가 죽었는데도 딜이 안 변하는 이유는
`armor_break_dmg_pct`가 **이 스쿼드에서 애초에 no-op**이기 때문이다 — 이 stat은 `is_armor_break_damage`
히트에만 가산되는데(⑤, `IMPL-STATUS.md`) 표준 스쿼드에 `armor_break_enabled` 공급원이 없다.
같은 이유로 `시크릿 가든 2`(42.39)도 전 단계에서 딜에 안 실린다.

**따라서 표준 스쿼드의 단계 상승분은 전부 `atk_caster_based_pct`에서 나온다** — 2단계 `아이리스 3`(45.12),
3단계 `시크릿 가든 3`(85.86).

**변형 3(방무 딜러 치사토 포함)으로 확인하면 0↔1단계가 갈라진다**: 치사토 딜 820.96M → 866.24M
(**+5.5%**). 즉 스킬2 사망은 실제로 딜 손실이며, 표준 스쿼드에서 안 보였을 뿐이다.
플로라의 애장품 가치를 볼 때는 반드시 방무 딜러를 낀 스쿼드로 판단한다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| 아이리스 2 (`event:adjacent_hp_max`) | 「최대 체력 도달」은 **hp_pct가 100% 미만이었다가 100%로 복귀하는 전이(edge)**로 해석. 상시 만피 상태에서는 반복 발동하지 않는다 | 인게임 문구 "도달 시" + 유저 확정(피튜니아 3 만료가 발동원) | 레벨(상시 만피=참)로 보면 `battle_start`부터 영구 활성 → def_ignore 30.97%가 전투 내내 켜져 **딜이 크게 과대평가**된다 |
| 피튜니아 3 (`hp_only_caster_based_pct`) | **시전자(플로라) base_hp 기준** 15.01%를 인접 아군 최대 체력에 flat 가산. 현재 체력은 유지 → 수령자의 hp_pct가 하락 | IMPL-STATUS `hp_only_caster_based_pct` 정의 | 수령자 자기 최대체력 기준이면 하락폭이 정확히 1/1.1501로 고정되지만, 시전자 기준이면 수령자별 하락폭이 달라져 **아이리스 2 발동 시점이 대상별로 갈릴 수 있다** |
| 아이리스 2 + 시크릿 가든 2 | 서로 다른 name의 독립 버프 → `armor_break_dmg_pct` **합산 73.36%** | 파싱상 별도 항목, name 중복 없음 | 동일 stat 최대값만 적용이면 42.39%만 실려 버스트 사이클 딜이 낮아진다 |
| 시크릿 가든 3 (`atk_caster_based_pct` 85.86) | 시전자 플로라의 **최종 공격력** × 85.86%를 아군 `atk_flat`에 가산 (버프 반영 후 시전자 ATK 기준) | IMPL-STATUS `atk_caster_based_pct` 후처리 정의 | 수령자 자기 공격력 기준(`atk_pct`)이었다면 지원형 플로라의 낮은 ATK가 아니라 딜러 ATK에 곱해져 **훨씬 큰 버프**가 된다. 텍스트가 "시전자 기준"이므로 현 해석 유지 |
| 피튜니아 3 대상 (`allies_adjacent:2`) | 원문 「마음의 평화 상태인 아군 전체」를 **자신+양 옆 2기**로 치환. 마음의 평화 수령자 집합과 동일하기 때문 | 마음의 평화 = `allies_adjacent:2`로 부여, instant는 `self_state` 앵커 불가 | 문자 그대로 상태 참조를 구현했다면 결과는 동일. 다만 아군 사망·상태 해제 모델이 생기면 갈라진다 |
| `burst_enter:2` 발동 주체 | 스쿼드의 **누가** B2를 눌러도 플로라에게 발동 (GAMEPLAY §timing) | `burst_enter:N` 일반 규칙 | 본인 시전 한정이었다면 아이리스 2가 **2사이클당 1회**로 반감된다 |

## impl/bug-fix용 검증 체크리스트

- [ ] `event:adjacent_hp_max` notify 구현 — `hp_only_caster_based_pct`/`max_hp_only_pct` 만료 후 `sync_hp` 시점에 hp_pct 100% **재도달 전이**에서만 발생 (상시 만피 반복 발동 금지)
- [ ] 피튜니아 3가 **매 사이클** B2 진입 시 `allies_adjacent:2`(플로라+리머+크라운)에 2초 부여
- [ ] 그 만료 시 아이리스 2 `armor_break_dmg_pct` 30.97이 `all_allies`에 10초 발동 — **본인 버스트 미사용 사이클 포함 매 사이클**
- [ ] 버스트 사이클에 시크릿 가든 2(def_ignore 42.39) + 시크릿 가든 3(atk_caster 85.86) 10초 적용
- [ ] 아이리스 2와 시크릿 가든 2가 겹치는 구간에서 `armor_break_dmg_pct` **73.36 합산** 확인
- [ ] 시크릿 가든 3종은 **2사이클당 1회**만 발동 (쿨 40s)
- [ ] 본인 버스트 미사용 사이클에 시크릿 가든 효과 잔존 없음 (네거티브)
- [ ] 피튜니아 3으로 인접 아군이 90% 이하가 되면 아이리스 보호막이 아군 전체에 10초 적용
- [ ] 보호막 적용 시 아이리스 3 `atk_caster_based_pct` 45.12가 플로라+리머+크라운에 10초 적용
- [ ] `heal_received_pct`·`buff_max_stack_add`가 DPS에 영향 없음 (네거티브)
- [ ] 변형 1 배치에서 인접 대상이 크라운·test_B3로 바뀜
- [ ] 사이클 간격 기대 12.5s ±

### 애장품 단계별 (2026-08-17 추가)

- [x] 0~3단계 전부 `char_effects()`가 끊기지 않고 효과 7·8·9·10개를 조합
- [x] 스킬1 이름 시프트가 단계 경계에서만 일어난다 — 0단계 `피튜니아`=주기 회복,
      1단계 이상 `피튜니아`=받는 회복량. 발동 횟수로 대조(주기 회복 1회 / 받는 회복량 3회)
- [x] **0단계 네거티브**: `아이리스`·`아이리스 2`·`아이리스 3` 발동 0회
      (`hp_only_caster_based_pct` 부재 → HP% 전이 없음. sim 한계로 확정, 파싱 오류 아님)
- [x] 1단계 이상에서 `아이리스`/`아이리스 2`가 3단계와 같은 횟수(220/210)로 발동
- [x] 표준 스쿼드에서 0단계와 1단계의 총딜이 같다 — `armor_break_dmg_pct`가 no-op이라서
- [x] **변형 3(방무 딜러)에서는 0↔1단계가 갈라진다** — 치사토 820.96M → 866.24M (+5.5%)
- [x] 단계별 스쿼드 총딜 단조 증가: 2,171.3M / 2,171.3M / 2,421.3M / 2,742.8M

> **구현 메모 (2026-08-10, 보호막 모델 추가)**: 아이리스 보호막과 아이리스 3 연계 PASS.
> 아이리스 2는 양 옆 아군 2명이 같은 프레임에 만피 복귀하므로 **프레임당 2회 활성화**되지만
> `max_stack: 1`이라 갱신될 뿐 중복 적용은 없다 — 로그에서 2회로 보여도 정상.
