# 아인 동작 시나리오

> 모드: 보강

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: 40s
- 무기 유형: SR (차지형, 장탄 6, 재장전 2.0s, 차지 1.0s, 풀차지 250%, 코어 200%)
- 속성/클래스/기업: 전격 / 화력형 / 미실리스 (스쿼드: 스쿨 서클)
- 작성 사이클 수: 2 (쿨 40s)

---

## 니어 페더 메커니즘 (스킬 텍스트 밖 — 커뮤니티 자료)

**게임 내 스킬 설명에 페더의 지속시간·공격 주기·쿨감 계산식이 전혀 없다.** 아래는 커뮤니티
자료로 확정한 것으로, 이 문서가 유일한 출처다. 구현·수정 시 반드시 여기를 근거로 삼는다.
수치는 코드 상수가 아니라 `parsed_skills.json`의 `feather_refresh` 효과 필드에 있다
(`feather_slots` · `feather_interval_base` · `feather_interval_mult`) — 추정치가 검증되면
JSON만 고치기 위함.

### 출처와 신뢰 순서
1. 나무위키 — "니어 페더는 통상 총 34회 공격하나, 버스트 중에는 실질적으로 31회 정도 공격한다."
2. 클뜯 커뮤니티 본문 (2024-07-28) — 소환 수·개별 지속시간·8초 주기·16% 감소
3. 위 글 댓글 — 합연산 주장 (**기각**, 아래 참조)

### 확정 규칙

| 항목 | 값 |
|---|---|
| 소환 (스킬1) | 전투 시작 시 페더 **1·2·3·4** 4기 |
| 소환 (버스트) | 페더 **1~6** 6기. **1~4는 재소환** — 이미 있던 페더도 지속시간·공격 쿨이 초기화 |
| 지속시간 | 1번 **무제한** / 2번 38s / 3번 32s / 4번 26s / 5번 10s / 6번 10s |
| 공격 주기 | 기본 8초. 생존 페더 수 n에 대해 **`8 × 0.84^(n-1)` (곱연산)** |
| 첫 공격 | 소환 즉시가 아니라 **주기만큼 뒤** (유저 확인) |
| 쿨 초기화 | **버스트 재소환만** 진행 중 쿨타임을 초기화. 만료로 페더 수가 줄어도 **이미 예약된 다음 틱 시각은 그대로**이고, 그 다음 틱부터 새 주기가 적용된다 (유저 확인) |
| 발사 형태 | 6기가 분산 발사. 단일 보스 sim이라 총딜에는 무영향 — 계산기는 **1틱에 생존 수만큼 동시 발사**로 모델링 (유저 확인) |
| 체공(비행) 시간 | 별도 모델링하지 않음 — 나무위키 34→31 차이에 이미 반영된 것으로 간주 (유저 확인) |

주기표:

| 생존 페더 | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| 공격 주기 | 8.00s | 6.72s | 5.65s | 4.74s | 3.98s | 3.35s |

### 곱연산 채택 근거 (합연산 기각)

커뮤니티 본문은 "곱연산인 것 같은데 검증 안 해봤음", 댓글은 "합이 맞다(2.88s 실측)"로 갈렸다.
나무위키의 타격 횟수로 갈랐다 — 버스트 소환 6기 세트가 만료될 때까지의 총 타격 수:

| 가설 | 6기 주기 | 총 타격 | 나무위키(34/31) |
|---|---|---|---|
| 합 `8×(1−0.16(n−1))` | 1.60s | **57회** | ✗ 불일치 |
| 합·자기 포함 `8×(1−0.16n)` | 0.32s | 218회 | ✗ 붕괴 |
| **곱 `8×0.84^(n−1)`** | 3.35s | **33회** | ✓ 34에 정합, 체공·다음 버스트 리셋 감안 시 31 |

합연산은 6기 구간(0~10초)에서만 36회를 때려 나무위키 총합을 혼자 넘긴다.
댓글의 2.88s는 합연산 **5기** 상태의 값인데, 페더 5·6번은 지속시간이 둘 다 10초라 **동시에
만료**되므로 5기 구간 자체가 존재하지 않는다 — 측정 대상이 없는 값이라 기각한다.

---

## 효과 요약 (파싱 결과 — `data/parsed_skills.json` 8항목)

수치는 스킬 레벨 10 기준. 배열 순서 = 원문 `■` 블록 순서 (실행 순서와 직결, §스킬 간 상호작용).

| ID (name) | 출처 | 트리거 | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|
| `페더 스탠바이` | 스킬1 | `battle_start` | `feather_refresh` — 슬롯 `[-1, 38, 32, 26]` | self | — (instant) |
| `페더 스탠바이 2` | 스킬1 | `burst_enter:3` | `atk_pct: 70.12` | self | 10s |
| `니어 페더 공격` | 스킬2 | `feather_tick` | `armor_break_damage:니어 페더` — 최종공격력 `90.81` | `enemies_random:1` | — |
| `페더 샷` | 스킬2 | `full_charge_hit` | `charge_dmg_pct: 80.0` | self | 1발 |
| `페더 올레인지` | 스킬3 | `burst_cast` | `feather_refresh` — 슬롯 `[-1, 38, 32, 26, 10, 10]` | self | — (instant) |
| `페더 올레인지 2` | 스킬3 | `burst_cast` | `armor_break_dmg_pct: 55.3` | self | 10s |
| `페더 올레인지 3` | 스킬3 | `burst_cast` | `charge_dmg_pct: 140.68` | self | 10s |
| `페더 올레인지 4` | 스킬3 | `burst_cast` | `armor_break_damage` — 최종공격력 `300.02` | `enemies_top_def:10` | — |

신설 키 3종은 전부 미구현(❌) — `feather_refresh`(instant stat) · `feather_tick`(timing) ·
`armor_break_damage:[소환체명]`(동적 히트 수). 로스터·구현상태 정본은 `IMPL-STATUS.md`.

---

## 검증 스쿼드

표준 1개로 충분하다. 아인 스킬에는 `no_burst1_ally` · `back_row` · 아군 무기/클래스/코드
조건이 전혀 없어 조건부 변형이 필요 없다. 본인 버스트 미사용 사이클은 쿨 40s 특성상
표준 스쿼드 2사이클 타임라인에 자동으로 포함된다.

- **표준** (GAMEPLAY §표준 테스트 스쿼드 — 대상이 B3·쿨 40초):
  `["리틀 머메이드", "크라운", "아인", "test_B3"]`

---

## 사이클 타임라인

표준 스쿼드 · seed 42. 버스트·풀버스트 시각은 실측
(`python -m context.sim "리틀 머메이드,크라운,아인,test_B3" --view burst --seed 42`),
페더 볼리는 위 확정 규칙으로 계산한 기대값이다.

사이클 주기 **12.533s**, 아인 버스트는 **2사이클마다 = 25.067초 간격**
(t=3.200 / 28.267 / 53.333 / 78.400 / …).

| t | 이벤트 | 발동 효과 (ID) | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0 | battle_start | `페더 스탠바이` — 페더 4기, 다음 틱 예약 t=4.742 | self | |
| 3.200 | **아인 burst_cast** | `페더 올레인지` → `페더 올레인지 2`(`armor_break_dmg_pct` 55.3) → `페더 올레인지 3`(`charge_dmg_pct` 140.68) → `페더 올레인지 4`(딜 300.02) | self 10s / `enemies_top_def:10` | **t=4.742 예약 틱은 재소환에 취소** — 전투 시작 4기는 한 번도 못 쏜다 |
| 3.200 | `burst_enter:3` | `페더 스탠바이 2` (`atk_pct` 70.12) | self 10s | |
| 3.250 | full_burst 시작 | — | | 버스트 딜(3.200)은 **이 앞이라 풀버스트 +50% 미적용** |
| 6.546 | `feather_tick` | `니어 페더 공격` ×**6** | 적 | 풀버스트 O · 버스트버프 O |
| 9.891 | `feather_tick` | `니어 페더 공격` ×**6** | 적 | 풀버스트 O · 버스트버프 O |
| 13.200 | 페더 5·6번 만료 | 6기 → 4기 | | **예약된 13.237 틱은 그대로** |
| 13.200 | 버프 만료 | `페더 올레인지 2·3`, `페더 스탠바이 2` | | |
| 13.237 | `feather_tick` | `니어 페더 공격` ×**4** | 적 | 풀버스트 O · 버스트버프 **X** (경계) |
| 13.250 | full_burst 종료 | | | |
| 15.733 | test_B3가 B3 사용 | `페더 스탠바이 2` (`atk_pct` 70.12) | self 10s | 남이 써도 발동 |
| 17.979 | `feather_tick` | ×**4** | 적 | 풀버스트 O · 버프 X |
| 22.720 | `feather_tick` | ×**4** | 적 | 풀버스트 O · 버프 X |
| 27.462 | `feather_tick` | ×**4** | 적 | 풀버스트 **X** · 버프 X |
| 28.267 | **아인 burst_cast** (3사이클) | 3.200과 동일 — 6기 재소환·쿨 초기화 | | 페더 4번 만료(29.2) 직전 리셋 |
| 상시 | 풀차지 명중 | `페더 샷` (`charge_dmg_pct` 80, 1발) | self | 2발째부터 상시 |

**아인 버스트 1주기(3.200 → 28.267, 25.067s): 볼리 6회 · 페더 히트 28회.**
180초 전체: 볼리 42회 · 히트 **196회** (풀버스트 중 **168** · 버스트버프 창 **84**).

**핵심 관찰 — 페더 2·3·4번의 긴 지속시간은 표준 스쿼드에서 사문(死文)이다.**
아인 버스트 간격 25.067s < 페더 4번 지속 26s이므로, 실전 루프에서 페더 수는
**6기(약 10초) ↔ 4기(약 15초)** 만 왕복한다. 3기·2기·1기 구간은 버스트를 오래 못 쓰는
경우(쿨감 없는 스쿼드·초반 지연)에만 나타난다.

> **초안에서 정정된 것**: 초안은 "첫 페더 공격이 t≈4.74s"라고 적었으나, 표준 스쿼드에서는
> 아인 버스트가 t=3.200에 먼저 들어와 그 예약 틱을 취소한다. **전투 시작 4기는 실제로 한 발도
> 쏘지 않는다.** 첫 볼리는 버스트 후 t=6.546이다.

---

## 스킬 간 상호작용

- **`페더 올레인지 2`(방무▲ 55.3%)가 같은 버스트의 `페더 올레인지 4`(딜) 에 적용된다**
  - 경로: `armor_break_dmg_pct` 버프 → 같은 프레임 `armor_break_damage` 계산에 합산
  - 근거: GAMEPLAY §효과 실행 순서 — 원문 `■` 블록 서술 순서가 곧 실행 순서. 버프 블록이 딜보다 앞
  - 파싱 제약: `parsed_skills.json` 배열에서 `페더 올레인지 2`가 `페더 올레인지 4`보다 **앞**이어야 한다. 뒤집히면 버스트 딜이 조용히 55.3%p 낮아진다

- **`페더 스탠바이 2`(atk_pct)·`페더 올레인지 2`(방무▲)가 `니어 페더 공격`을 증폭한다**
  - 경로: `atk_pct` → 페더 딜의 "최종 공격력" 기반값 / `armor_break_dmg_pct` → 방무딜 계수에 합산
  - 시점: 두 버프 창(버스트 +10초)에 들어가는 볼리는 **t=6.546·9.891 두 번, 12히트**뿐이다
  - 위험: **t=13.237 볼리가 경계에 걸린다** — 버프는 13.200에 만료되고 풀버스트는 13.250에 끝나서, 이 4히트는 풀버스트 +50%는 받고 버스트 버프는 못 받는다. 프레임 처리가 한 틱만 어긋나도 4히트의 버프 적용 여부가 뒤집히므로 구현 후 반드시 확인

- **차지 대미지▲는 페더에 적용되지 않는다**
  - `페더 샷`(80%)·`페더 올레인지 3`(140.68%)의 `charge_dmg_pct`는 **풀차지 일반 공격 전용**
  - `니어 페더 공격`은 스킬 딜이라 코어 배율(200%)·풀차지 배율(250%)·`charge_dmg_pct` 모두 미적용
  - 즉 아인은 **차지딜 라인**(일반공격: `charge_dmg_pct` 2종)과 **페더 라인**(스킬딜: `atk_pct`·`armor_break_dmg_pct`)이 서로 다른 버프를 먹는 이중 구조다. 버프를 양쪽에 다 먹이는 것이 이 캐릭터의 대표적 오구현 경로

- **`feather_refresh`는 소환과 쿨 초기화를 한 항목이 함께 한다**
  - 재소환 프레임에서 **슬롯 만료 시각 갱신 → 다음 틱 재예약** 순서. 둘을 분리하면 옛 예약이 살아남아 볼리가 한 번 더 샌다
  - 순환 없음: 페더 수가 주기를 정하지만 주기는 페더 수를 바꾸지 않는다

---

## 유저 확인 사항

- Q: 페더 수 변동 시 진행 중인 쿨타임이 재계산되는가?
  A: **버스트 스킬 사용(재소환)만** 진행 중 쿨타임에 영향을 준다. 만료로 수가 줄어도 이미 예약된
  다음 틱 시각은 그대로이고, 그 다음 틱부터 새 주기가 적용된다.

- Q: 페더의 체공(비행) 시간을 모델링하는가?
  A: 하지 않는다. 나무위키 "통상 34회 / 버스트 중 실질 31회"의 차이에 이미 반영된 정보로 본다.

- Q: 6기가 동시에 쏘는가 분산해서 쏘는가?
  A: 분산 발사. 단일 적 sim이라 총딜에는 영향 없어 1틱 동시 발사로 모델링한다.

- Q: 페더의 첫 공격 시점은 소환 즉시인가 주기만큼 뒤인가?
  A: **주기만큼 뒤**. 즉시 1회 발동이면 버스트 6기 세트가 39회가 되어 나무위키 34회를 크게 넘는다.

- Q: 스킬2 `[차지 대미지 80% ▲] [1발 유지]`의 적용 범위는?
  A: **다음 1발부터**. 버프를 유발한 그 풀차지 발에는 미적용, 다음 발에 적용 후 소모.
  매 풀차지마다 재부여되므로 실질 2발째부터 상시 적용된다.

- Q: 페더 공격의 딜 분류는?
  A: **스킬 딜**. 최종 공격력·`armor_break_dmg_pct`·`atk_pct`는 적용, 코어 200%·풀차지 250%·
  `charge_dmg_pct`는 미적용. `hit_count` 등 일반공격 명중 트리거에도 카운트되지 않는다.

- Q: 신규 키 이름과 수치의 위치는?
  A: **`feather_*` (아인 전용)** 명명, 수치는 **`parsed_skills.json` 필드**에 둔다.

- Q: `페더 샷`이 한 발에도 적용되지 않던 문제(단계 4에서 발견)는?
  A: **공통 경로를 고쳤다** (유저 승인). `full_charge_hit`이 `buff_manager._BULLET_BOUND_TIMINGS`에
  들어 있어, 명중 후 붙은 버프가 이미 계산이 끝난 그 발에 1발을 소모당해 사라지고 있었다.
  집합에서 제거 — 회귀 25/25 통과, 같은 조합을 쓰는 라플라스 : 얼티밋 히어로 딜도 불변.
  일반 규칙으로 `GAMEPLAY.md §효과 실행 순서`에 승격했다. 아인 총딜 313.9M → **395.0M**.

- Q: 버스트 딜(`페더 올레인지 4`)이 풀버스트 +50%를 못 받는 것은?
  A: **현행 사양이 맞다** (유저 확인). `burst_cast` 딜을 풀버스트까지 보류하는 처리는
  `bonus_damage`에만 걸려 있고, 이는 `armor_break_damage`를 쓰는 모든 B3에 공통인 문제라
  아인 작업에서 단독으로 바꾸지 않는다.

### 일반 vs 특이 분류

페더 관련은 전부 **아인 특이 사항**이다 (고유 소환물의 동작).
**GAMEPLAY 승격 1건** — `duration_bullets` 버프를 1발로 셀지는 그 트리거가 `calc_damage`보다
앞서는가로 갈린다(`§효과 실행 순서`). 위 `페더 샷` 항목 참조.
"블록 서술 순서 = 실행 순서", "`burst_enter:3`는 남이 써도 발동"은 이미 GAMEPLAY에 등재된
일반 규칙을 적용한 것이라 추가 등재하지 않는다.

---

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| `feather_tick` 주기 | `8 × 0.84^(n-1)` **곱연산** | 나무위키 34/31회 ↔ 시뮬 33회 정합 | 합연산이면 6기 주기 1.60s → 히트 수가 **2배 이상**(사이클 28 → 60+). 딜이 통째로 어긋난다 |
| `feather_tick` 첫 타 | 소환 **+주기** 뒤 | 유저 확인. 즉시면 6기 세트 39회로 34회 초과 | 즉시면 사이클당 볼리 1회(6히트) 추가 |
| 만료 시 예약 틱 | **불변**. 다음 틱부터 새 주기 | 유저 확인 ("버스트 사용만 진행 중 쿨에 영향") | 즉시 재계산이면 6→4 전이에서 t=13.237 볼리가 밀려 풀버스트 밖으로 나갈 수 있다 |
| `armor_break_damage:니어 페더` | 생존 수만큼 **개별 히트 발사** | 크리는 히트마다 판정되고, 34회 정합 검증도 히트 수로 한다 | 1히트 ×n 계수면 기대딜은 같지만 크리 분산·히트 수 집계가 달라져 체크리스트 검증이 불가능해진다 |
| `페더 올레인지 4` (버스트 딜) | 같은 버스트의 **방무▲ 55.3% 적용받음** | GAMEPLAY §효과 실행 순서 (블록 순서) | 미적용이면 버스트 딜만 약 −36% |
| `페더 올레인지 4` | 풀버스트 **+50% 미적용** | 유저 확인 — 현행 사양 유지 | 적용하면 버스트 딜 +50%. B3 공통 사안이라 별건 |
| `니어 페더 공격` | **스킬 딜** — 코어·풀차지·차지▲ 미적용 | 유저 확인 | 일반공격 판정이면 코어 200%·풀차지 250%가 실려 페더 딜이 몇 배로 뛴다 |
| `페더 샷` | `duration_bullets: 1` — **다음 1발부터** | 유저 확인 | 즉시 적용이면 첫 풀차지도 +80% |
| `니어 페더 공격` target | `enemies_random:1` | 원문 "무작위 적 1기에게" | 단일 보스라 실질 동일 (센티널 반환) |

---

## impl/bug-fix용 검증 체크리스트

표준 스쿼드 `["리틀 머메이드", "크라운", "아인", "test_B3"]` · seed 42 · 180초 기준.

**페더 궤적**
- [ ] 아인 버스트 시각: 3.200 / 28.267 / 53.333 / 78.400 / 103.467 / 128.533 / 153.600 (간격 25.067s)
- [ ] 첫 페더 볼리는 **t=6.546** — `t=4.742`에 볼리가 있으면 재소환이 예약 틱을 취소하지 않은 것
- [ ] 첫 2사이클 볼리 시각·히트: 6.546(**6**) / 9.891(**6**) / 13.237(**4**) / 17.979(**4**) / 22.720(**4**) / 27.462(**4**)
- [ ] 아인 버스트 1주기당 볼리 **6회 · 히트 28회**
- [ ] 180초 총 페더 볼리 **42회 · 히트 196회** (`analyze_damage()`의 `니어 페더 공격` 히트 수)
- [ ] 페더 수가 **6 ↔ 4만 왕복** — 3기 이하 구간이 나타나면 버스트 사용 간격 또는 슬롯 만료 처리를 의심
- [ ] 페더 수 6기 구간 간격 **3.346s**, 4기 구간 **4.742s**
- [ ] t=13.200 만료(6→4) 이후에도 다음 틱은 **13.237 그대로** (재계산되어 앞뒤로 움직이면 안 됨)

**버프 적용**
- [ ] 페더 히트 중 풀버스트 안 **168회** / 버스트버프(`페더 올레인지 2`) 창 안 **84회**
- [ ] **t=13.237 볼리 4히트는 풀버스트 O · 버스트버프 X** (경계 케이스 — 여기가 뒤집히면 구현이 어긋난 것)
- [ ] `페더 스탠바이 2`(`atk_pct` 70.12)가 아인 버스트 사이클과 test_B3 사이클 **양쪽 모두** 발동 (사이클당 1회)
- [ ] `페더 샷`(`charge_dmg_pct` 80)이 **첫 풀차지 발에는 미적용**, 2발째부터 적용
- [ ] `페더 올레인지 4`(버스트 딜)가 `페더 올레인지 2`의 방무▲ 55.3%를 받은 값

**네거티브 (발동/적용되면 안 되는 것)**
- [ ] `니어 페더 공격`에 코어 배율(200%)·풀차지 배율(250%)·`charge_dmg_pct`가 실리면 안 됨
- [ ] `니어 페더 공격`이 일반공격 명중 카운터(`hit_count` 계열)를 밀면 안 됨
- [ ] `페더 올레인지 4`는 풀버스트 +50%를 받지 않는 것이 **현행 정답** (받으면 회귀)
- [ ] `enemies_top_def:10`이 단일 보스에서 **1히트** (10배 아님)

**사이클**
- [ ] 사이클 간격 12.533s · 180초 풀버스트 14회
- [ ] 참고 정합: 버스트 소환 6기 세트를 끝까지 살리면 총 타격 **33회 내외** (나무위키 34회)
