# 팬텀 동작 시나리오

> 모드: **보강** (`/char-parse` 후) · **`/char-impl` 완료 반영**.
> 수치는 전부 표준 스쿼드 seed 42 · 작열 보스 실측 확정값이다.
>
> 재현: `python -m context.sim "리틀 머메이드,크라운,팬텀,test_B3" --seed 42 --enemy-code 작열`

## 기본 정보
- 버스트 단계: B3 / 본인 쿨다운: 40s → **2사이클 작성**
- 무기 유형: AR (최대 장탄 60, 재장전 2.00s, 계수 15.78, 코어 200%, fire_rate 12/s)
- 코드/클래스/기업: **수냉** / 화력형 / 엘리시온
- `post_fire_delay` 0 / `post_reload_delay` 0
- 애장품 **`소중한 가면들`** (단계1→스킬1, 단계2→스킬2, 단계3→스킬3).
  **0~3단계 전 판본 파싱 완료** (2026-08-17). 아래 효과 요약·타임라인·상호작용은 **3단계 기준**이며,
  낮은 단계 차이는 `## 애장품 단계별 차이` 참조

**한 줄 요약**: 예고장(적 방깎) ↔ 괴도의 단검(자기 스택)이 맞물려 도는 자가 루프형 딜러.
루프 한 바퀴마다 추가 대미지 + AoE 분배 대미지가 터지고, 그 횟수만큼 **분배 대미지 스택**이
쌓여 자기 버스트(최종공격력 1457.28% 분배 대미지)를 키운다.

---

## 효과 요약 (파싱 결과 — Lv10 기준, 배열 순서대로)

| # | ID (`parsed_skills.json` name) | 출처 | 트리거 | stat / 값 | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 1 | 괴도의 단검 | 스킬1 | `hit_count:1` + `not_target_state:예고장` | `accuracy_pct` 25.75 (max 3) | self | 5s |
| 2 | 예고장 | 스킬1 | `hit_count:1` + `not_target_state:예고장` | `def_pct` **−32.19** | target | 5s |
| 3 | 괴도의 단검 2 | 스킬1 | `hit_count:30` | `buff_stack_add` +1 → `괴도의 단검` | self | — |
| 4 | 괴도의 예고장 | 스킬1 | `hit_count:1` + `target_state:예고장` | `atk_dmg_pct` 75.17 | self | 1발 |
| 5 | 괴도의 시선 | 스킬2 | `hit_count:1` + `self_stack_above:괴도의 단검:3` + `target_state:예고장` | `bonus_damage` 84.33 | target | — |
| 6 | 괴도의 시선 2 | 스킬2 | 〃 | `remove_named_buff` → `예고장` | target | — |
| 7 | 괴도의 시선 3 | 스킬2 | `stack_reach:괴도의 단검:3` | `split_dmg_pct` 12.86 (max 3) | self | **지속** |
| 8 | 괴도의 시선 4 | 스킬2 | `hit_count:1` + `self_stack_above:괴도의 단검:3` | `split_damage` 250 | all_enemies | — |
| 9 | 괴도의 시선 5 | 스킬2 | 〃 | `remove_named_buff` → `괴도의 단검` | self | — |
| 10 | 괴도의 시선 6 | 스킬2 | `hit_count:10` | `atk_pct` 85.12 | self | 5s |
| 11 | 괴도의 시선 7 | 스킬2 | `hit_count:10` | `split_dmg_pct` 31.92 | self | 10s |
| 12 | 비기 괴도 난무 | 스킬3 | `burst_cast` | `split_damage` 1457.28 | all_enemies | — |
| 13 | 비기 괴도 난무 2 | 스킬3 | `burst_cast` + `target_code:작열` | `received_dmg_pct` 18 | same_target | 30s |
| 14 | 비기 괴도 난무 3 | 스킬3 | `burst_cast` | `max_ammo_pct` 50 | self | 10s |
| 15 | 괴도의 시선 8 | 스킬2 | `burst_cast` | `remove_named_buff` → `괴도의 시선 3` | self | — |

**배열 순서가 곧 실행 순서다.** #1→#2, #5~#9 그룹 내 순서, #12→#15 셋 다 결과를 바꾼다 (§스킬 간 상호작용).

구현상태 (IMPL-STATUS 대조) — **`/char-impl`에서 3건 구현 완료**:
- **`not_target_state:상태명`** ✅ 신규 구현. `_condition_ok`·`_runtime_condition_ok`에 분기 추가,
  `_RUNTIME_COND_PREFIXES` 등록, `_has_target_state()` 단일 창구로 `target_state:`와 공유.
- **`buff_stack_add`** ✅ 두 가지 보강 — ① 대상 버프의 **지속시간도 갱신**(유저 확정: 일반 동작),
  ② 스택 증가 시 **`stack_reach:버프명:N` notify** 발생(`_activate()`와 동일 규칙).
  기존 사용처(마르차나·라피·디젤·소다)는 전부 대상 버프가 `duration: -1`이라 **영향 0**(회귀 20/20 PASS, 마르차나 diff 0).
- `accuracy_pct` ⚠️ — `core_px > 0`일 때만 코어히트율에 반영. 기본 보스(코어 없음)에서 **#1은 DPS 무영향**
  (명중률 값을 0으로 바꿔도 딜이 736,297,480으로 동일함을 실측 확인).
- 나머지 stat·target·timing은 전부 ✅ 완전 구현.

---

## 검증 스쿼드

아군 구성(버스트 단계·무기·클래스·코드) 조건 효과가 **하나도 없어 스쿼드 변형은 불필요**하다.
보스 설정만 변형 대상이다.

- **표준**: `["리틀 머메이드", "크라운", "팬텀", "test_B3"]` + `enemy={"code": "작열"}`
  - 팬텀은 B3 중 맨 앞 → `test_B3`와 교대로 **2사이클당 1회** 버스트
  - 수냉→작열 **우월 코드**(DealForm ⑦) + #13 `target_code:작열` 조건을 함께 검증
- `context/sim.py --enemy-code 작열` ✅ 추가 완료 (`--enemy-def`/`--core-px` 옆).
- **코어**: 기본값 `core_px = 0` 유지(유저 확정). #1 명중률의 실효를 보려면 `--core-px 50` 별도 실행.

---

## 사이클 타임라인 (seed 42 실측, 2사이클)

`h###` = 팬텀 누적 일반 공격 횟수. 사이클 간격 **12.533s**, 풀버스트 10s.

| t | 이벤트 | 발동 효과 | 대상/지속/스택 | 비고 |
|---|---|---|---|---|
| 0.000 | h1 | #1 괴도의 단검 1중첩 → #2 예고장 → #4 괴도의 예고장 | self 5s / target 5s / 1발 | **#1이 #2보다 먼저**여야 성립 |
| 0.750 | h10 | #10 `atk_pct` 85.12(5s) + #11 `split_dmg_pct` 31.92(10s) | self | 이후 ~0.83s마다 재발동 → **상시 100%** |
| 2.433 | h30 | #3 → 괴도의 단검 2중첩 | self | **지속시간도 5초로 갱신**(§해석 선언 D) |
| **3.200** | **팬텀 버스트 (1회차)** | #12 분배 1457.28 → #13 받는딜 18(30s) → #14 최대장탄 50(10s) → #15 스택 제거 | — | **딜 11,924,600 — 분배 스택 0** |
| 3.250 | 풀버스트 시작 | — | — | |
| 4.933 | h60 | #3 → **3중첩** → #5 추가딜 1,197,364 / #6 예고장 제거 / #7 분배스택 **1** / #8 AoE 3,143,449 / #9 단검 해제 | — | **1회차 루프 완주** |
| 5.017 | h61 | #1 단검 1중첩 → #2 예고장 재부여(→10.02) | — | 공백 1프레임 |
| 10.050 | — | 3중첩 → #5 1,197,364 / #8 3,391,789 / #7 분배스택 **2** | — | |
| 12.967 | — | 3중첩 → #5 1,197,364 / #8 3,640,129 / #7 분배스택 **3 (만재)** | — | |
| 13.250 | 풀버스트 종료 | — | — | |
| 15.733 | **test_B3 버스트** | (팬텀 미사용 슬롯) | — | **#7 분배 스택 유지**(Q5 — 자신의 버스트만 제거) |
| 18.100 | — | 3중첩 → #5 1,308,797 / #8 4,148,251 | — | #7은 상한 3 유지 |
| 21.567 | — | 3중첩 → #5 1,030,299 / #8 3,231,503 | — | |
| 25.783 | 풀버스트 종료 | — | — | |
| 26.683 | — | 3중첩 → #5 589,017 / #8 1,712,807 | — | 풀버스트 밖이라 딜이 낮다 |
| **28.267** | **팬텀 버스트 (2회차)** | #12 분배 1457.28 **× 분배스택 3** → #15 제거 | — | **딜 17,039,016 = 1회차 × 1.4289** |
| 28.317 | 풀버스트 시작 | — | — | 스택 재적립 시작 |
| 29.617 | — | 3중첩 → #5 1,197,364 / #8 3,143,449 / #7 분배스택 **1** | — | 리셋 확인 지점 |

**버스트 딜 검산** — 1회차 factor⑥ = 1 + 0(받는딜) + 0.3192(#11) = **1.3192**,
2회차 = 1 + 0.18(#13) + 0.3192(#11) + 0.3858(#7×3) = **1.885**.
1.885 / 1.3192 = **1.4289**, 실측 17,039,016 / 11,924,600 = **1.4289** ✅ 정확히 일치.

**8회 버스트 딜 열**: `11.92M → 17.04M → 18.65M → 17.04M → 18.65M → **25.56M(크리)** → 18.65M → 17.04M`
(1회차만 스택 0, 이후 3스택 만재로 안정. 17.04↔18.65 교대는 아군 버프 위상 차, 6회차는 크리)

**최대중첩 proc**: 180초 **42회**, 간격 **2.75~5.28s**(평균 4.16s).
`5.12 / 2.92 / 5.13 / 3.47 / 5.12 / 2.93 …` 패턴이 반복된다 — 탄창·재장전 위상에 물린 결과.

**180초 딜**: 팬텀 **736,297,480** / 스쿼드 **1,548,357,885** (**47.6%**).
효과별 — 기본공격 408.2M(55.4%) · #12 144.6M(19.6%) · #8 135.0M(18.3%) · #5 48.5M(6.6%).

---

## 스킬 간 상호작용

- **#7(분배 스택) → #12(버스트 분배 대미지)**
  - 경로: #1/#3 단검 스택 → 3중첩 → #7 +1(최대 3) → `burst_cast` 시 factor⑥에 합산 → #15가 제거
  - 시점: 만재까지 proc 3회 ≈ 21.6초. 팬텀 버스트 주기가 25.07초라 **2회차부터는 항상 만재**
  - 위험: **1회차 버스트는 스택 0**이다. 사이클 1 버스트 딜이 낮은 것이 정상
- **#2(예고장) ↔ #5·#6 순환** — #1·#2는 "예고장이 **없을 때만**" 붙고, #5는 "있을 때만" 발동하며 #6이 **제거**한다.
  예고장의 실질 유지율은 루프 주기의 역함수. 자기 강화가 팀 디버프(방깎)를 갉아먹는 구조
- **#3이 단일 보스 루프를 성립시킨다** — #1만으로는 예고장이 붙어 있는 동안 스택이 늘지 않아
  **단일 대상에서 3중첩에 절대 도달하지 못한다**. 애장품 1단계가 추가하는 #3이 유일한 상시 공급원
- **#14(최대 장탄 50%)가 루프 위상을 바꾼다** — 탄창 60→90, 버스트 후 10초만 적용 → 사이클마다 위상이 달라진다

### 동일 timing 실행 순서 (전부 배열 순서로 해소)

| 순서 제약 | 어기면 |
|---|---|
| **#1 괴도의 단검 → #2 예고장** | #2가 먼저면 같은 프레임에 `not_target_state:예고장`이 뒤집혀 **#1이 영원히 생성되지 않는다 → 스킬2 전체 무발동**(실측 확인) |
| **#2 예고장 → #5 괴도의 시선** | #5가 먼저면 방금 부여된 예고장을 못 읽어 추가 대미지가 빠진다 (유저 확정 Q1 위반) |
| **#5·#6·#8 → #9 단검 해제** | #9가 먼저면 `self_stack_above:괴도의 단검:3`이 깨져 나머지가 전부 무발동 |
| **#12 비기 괴도 난무 → #15 스택 제거** | #15가 먼저면 분배 스택이 버스트 대미지에 실리지 못한다 (유저 확정 Q2 위반). `split_damage`는 `bonus_damage`와 달리 `_pending_burst_dmg`로 보류되지 않고 `burst_cast`에 즉시 계산되므로 **#15를 스킬3 항목들보다 뒤에 배치**했다 |

---

## 유저 확인 사항

- **Q1. 같은 히트에서 `예고장 부여`와 `단검 3중첩 도달`이 겹칠 때 #5가 그 프레임에 성립하나?**
  - **A: 같은 프레임에 즉시 소비.** 예고장을 붙인 히트에서 곧바로 #5가 읽어 추가 대미지를 넣고 #6이 제거한다.
- **Q2. #7의 분배 스택이 팬텀 자신의 버스트(#12)에 실리나?**
  - **A: 버스트 대미지에 실린 뒤 제거.** 실측 검산으로 확인 완료(위 1.4289).
- **Q3. #7 `[3 중첩]`의 의미는?**
  - **A: 1회 발동당 1중첩, 최대 3.** 기존 `[N 중첩]` = `max_stack: N` 관례와 동일.
- **Q4. 검증 보스 설정은?**
  - **A: 작열 코드 보스 명시.** 코어는 기본값(없음) 유지.
- **Q5. #15 `[버스트 스킬 사용 시 중첩 제거]`의 주체는?**
  - **A: 팬텀 자신의 버스트만.** 아군 버스트로는 지워지지 않는다(t=15.733 test_B3 버스트에서 유지 확인).

> **GAMEPLAY 승격 여부**
> - Q1·Q2 → **거부됨**. 팬텀 한정 해석으로 이 문서에만 둔다. 다른 캐릭터에서 같은 패턴을 만나면 그때 다시 판단한다.
> - U1(스택 부여 시 지속시간 갱신) → **승격됨**. `GAMEPLAY.md §트리거 발동 의미 §버프 스택`.
>   유한 지속시간 스택 버프 전반에 적용되는 일반 규칙이다.

- **U1. `buff_stack_add`가 대상 버프의 지속시간도 갱신해야 하나?**
  - **A: 갱신한다. 이는 일반적인 작동 방식이다** (팬텀 한정 예외가 아님).
    → `_dispatch_instant()`의 `buff_stack_add` 핸들러에 지속시간 갱신을 넣었다.
    proc 23회 → **42회**, 팬텀 딜 645.6M → **736.3M (+14.0%)**.

- **U4. 인게임 스택 사이클이 계산기와 일치하는가?** (2026-08-16, 래치 전수 조사 중 대조)
  - **A: 유저 인게임 확인 — 보스전 기준 `첫 발에 1스택 → 30발마다 +1 → 3스택 도달 시 추가 대미지 넣고 0스택 →
    이후 한 발에 즉시 다시 1스택`.** 실측이 정확히 일치한다:

    | t | 누적 발수 | 상태 |
    |---|---|---|
    | 0.017 | 1 | 단검 1스택 + 예고장 부여 |
    | 2.450 | 30 | 단검 2스택 |
    | 4.950 | 60 | 3스택 도달 → `괴도의 시선` 딜(t=4.93) → 0스택, 예고장 제거 |
    | 5.033 | 61 | 단검 1스택 + 예고장 재부여 |

  - **`not_target_state:예고장`은 정당한 발동 게이트다 — 제거 대상이 아니다.** 3중첩 소비 시
    `괴도의 시선 2`가 예고장을 함께 지우므로 다음 한 발이 곧바로 조건을 통과한다.
    이 조건을 지우면 매 발 스택이 쌓여 딜이 +149% 튀는데, 그건 버그 수정이 아니라 설계 파괴다.
  - 예고장 디버프(적 방어력 −32.19%)도 정상 적용된다 — t20~40 구간 **98.1% 시간** 활성.

### 남은 사항 (딜 영향 없음)

- **U2. #4 `[1발 유지]`가 실리는 탄** — 계산기 일반 규칙(`_BULLET_BOUND_TIMINGS`에 `hit_count` 포함)을 따른다.
  예고장 활성 중 매 히트 재부여되므로 어느 쪽이든 유지율 ≈100% → 딜 영향 미미.
- **U3. #10·#11 `일반 공격 10회 "공격" 시`** — 다른 블록은 "명중 시"인데 여기만 "공격 시".
  계산기는 발사=명중이라 `hit_count:10`으로 동일 처리했다.

---

## 애장품 단계별 차이

| 단계 | 갈리는 슬롯 | 3단계 대비 없는 것 | 효과 수 |
|---|---|---|---|
| 0 | 전 슬롯 기본 | #3 `괴도의 단검 2` · #8 `괴도의 시선 4` · #13 · #14 | 11 |
| 1 | 스킬1 애장품 | #8 `괴도의 시선 4` · #13 · #14 | 12 |
| 2 | +스킬2 애장품 | #13 `비기 괴도 난무 2` · #14 `비기 괴도 난무 3` | 13 |
| 3 | +스킬3 애장품 | — | 15 |

기본 판본 스킬2는 애장품 2단계에서 `괴도의 시선 4`(적 전체 분배 대미지)만 빠져 **번호 4가 비어 있다**
(`PARSING.md` 판본 간 번호 정렬 규칙). 원문의 `중첩 해제 후 자신에게`는 애장품 판본과 실행 결과가
같아 `stack_reach:괴도의 단검:3`으로 얹고 해제(#9)를 그룹 마지막에 두는 순서 제약을 그대로 따른다.
`비기 괴도 난무`(#12) 값은 기본·애장품 3단계가 **1457.28로 동일**하다.

### 0단계에서 스킬2 루프가 구조적으로 돌지 않는다 (유저 확정 2026-08-17)

**기본 스킬1에는 #3 `괴도의 단검 2`(`hit_count:30` → 스택 +1)가 없다.** 그러면 `괴도의 단검` 스택의
공급원은 #1 하나뿐인데, #1의 조건이 `not_target_state:예고장`이고 **`예고장`(#2)과 `괴도의 단검`(#1)이
같은 프레임에 붙어 둘 다 5초**다. 즉 다음 스택은 **직전 스택이 만료된 뒤에야** 붙는다 —
구조적으로 1중첩을 못 넘는다. 실측 이벤트도 정확히 5초 주기다:

```
t= 0.000 ON 괴도의 단검 / 예고장     t= 5.017 ON 괴도의 단검 / 예고장
t=10.017 ON 괴도의 단검 / 예고장     t=15.083 ON 괴도의 단검 / 예고장
```

3중첩을 전제로 한 #5(추가 대미지)·#6·#7(분배 스택)·#9가 **180초 내내 0회**다. `괴도의 시선 3`이 없으니
버스트 분배 대미지도 스택 증폭 없이 1회차 값으로 고정된다.

**애장품 1단계가 이 메카닉을 푸는 열쇠다** — #3이 붙는 순간 30히트마다 스택이 공급되어 루프가 돈다.
해석 선언 **A**가 "스택이 2개로 분리되면 3중첩 영구 미달 → 스킬2 전체 무발동"이라 적은 그 상태가,
0단계에서는 스택 공급원 부재로 실제로 일어난다.

> **단일 보스 기준의 결론이다** (유저 확정). 인게임에서도 무애장품 팬텀은 이 루프가 돌지 않는다.
> 파싱 오류가 아니므로 고치지 않는다.

### 실측 (표준 스쿼드, seed 42, `--enemy-code 작열`, 180s)

| 단계 | 팬텀 딜 | 스쿼드 총딜 | #5 추가딜 | #8 분배딜 | #7 분배 스택 | #12 버스트 |
|---|---|---|---|---|---|---|
| 0 | 883.9M | 1,751.2M | **0** | — | **0** | 8 |
| 1 | 1,003.8M | 1,867.3M | 40 | — | 47 | 8 |
| 2 | 1,212.6M | 2,079.1M | 40 | 40 | 47 | 8 |
| 3 | 1,394.5M | 2,416.5M | 40 | 40 | 47 | 8 |

`괴도의 단검` 부여 횟수는 0단계 **71회** vs 1단계 **162회** — 차이 91회가 #3이 공급한 스택이다.
#10·#11(`hit_count:10` 자버프)은 전 단계 207회로 동일해, 죽은 것은 스택 루프뿐임을 확인한다.

---

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| **A. #3 괴도의 단검 2** | 별도 buff가 아니라 `buff_stack_add`(target_effect=`괴도의 단검`)로 **스택 통합** | `_activate()`는 기존 버프를 **effect dict identity**(`ab.effect is eff`)로 식별 — 이름이 같아도 별개 항목이면 ActiveBuff가 갈라진다 | 별도 buff였다면 스택이 2개로 분리되어 **3중첩 영구 미달 → 스킬2 전체 무발동** |
| **B. #5·#6·#8·#9 트리거** | `hit_count:1` + `self_stack_above:괴도의 단검:3` (매 히트 판정) | 히트 notify 패스의 **끝**에서 평가되어 같은 프레임에 부여된 예고장을 읽는다(Q1 충족). damage/instant라 런타임 재평가 무관 | `stack_reach` timing이었다면 `_activate` 중 **재진입 notify**로 발동해 예고장 부여 **전에** 판정 → #5 추가 대미지가 상시 누락 |
| **C. #7 트리거** | `stack_reach:괴도의 단검:3` + condition 없음 | `duration: -1` 버프에 `self_stack_above`를 걸면 `_RUNTIME_COND_PREFIXES` 재평가로 **같은 프레임의 중첩 해제와 함께 즉시 꺼진다**(PARSING §4-2 래치 규칙) | condition 방식이었다면 분배 스택이 **한 프레임도 유지되지 않아** 버스트가 1회차 딜(11.9M)로 고정 |
| **D. #3의 지속시간** | 스택 +1과 함께 **지속시간도 갱신**(유저 확정: 일반 동작) | 원문이 `[5초 유지]`가 붙은 버프를 다시 붙이는 문장. `_activate()`의 스택 갱신 규칙과 동일하게 맞췄다 | 미갱신이었다면 3중첩 전에 만료되는 구간이 생겨 proc 42회 → **23회**, 팬텀 딜 736.3M → **645.6M (−12.3%)** |
| **E. #2 예고장 부호** | `def_pct` **음수**(−32.19) | `▼` → values 음수 규칙. 적 대상이라 `enemy_def_down_pct`로 라우팅, `eff_def = def × (1 + pct/100)` | 양수였다면 적 방어력이 **증가**해 스쿼드 전체 딜이 떨어진다 |
| **F. #13 target** | `same_target` (헬름 : 아쿠아마린 `이지스 캐논 견제 사격 2`와 동일 패턴) | 원문 `동일 적 대상에게` + `target_code:작열` 조합이 선례와 일치 | `same_target:[name]`은 ❌ 미구현이라 무발동 |
| **G. #1 accuracy_pct** | 파싱은 정상 등록, **DPS 기여는 0** | `core_px = 0`(유저 확정 보스)에서 코어히트 확률 계산 자체가 없다 | 코어 보스였다면 탄착군 축소 → 코어히트율 ▲로 실딜 기여 |

---

## impl/bug-fix용 검증 체크리스트

`/char-impl` 전 항목 **PASS**. 아래 수치는 회귀 판정 기준선이다 — 어긋나면 무언가 깨진 것이다.

**선행 구현**
- [x] `not_target_state:상태명` — `_condition_ok` + `_runtime_condition_ok` 분기, `_RUNTIME_COND_PREFIXES` 등록
- [x] `buff_stack_add` → 지속시간 갱신 + `stack_reach:버프명:N` notify
- [x] `context/sim.py --enemy-code`

**루프 성립**
- [x] 괴도의 단검 3중첩 도달 — 180초 최대중첩 proc **42회**, 간격 2.75~5.28s(평균 4.16s)
- [x] proc마다 #5·#8이 세트로 1회씩 — 둘 다 **42회**
- [x] 괴도의 단검 스택 분포 `{0: 84, 1: 794, 2: 1227}` — **3중첩 프레임 0개**(도달 즉시 해제)
- [x] 예고장 유지율 **96.0%** (2021/2105 프레임)
- [x] 예고장이 동시에 2개 이상 활성화되지 않는다 — `_activate()`가 effect dict identity로 식별해 구조적 보장

**분배 스택 · 버스트**
- [x] 1회차 버스트(t=3.200) = **11,924,600** — 분배 스택 0
- [x] 2회차 버스트(t=28.267) = **17,039,016** = 1회차 × **1.4289**
- [x] 8회 버스트 딜 열 `11.92 / 17.04 / 18.65 / 17.04 / 18.65 / 25.56(크리) / 18.65 / 17.04` (M)
- [x] 버스트 8회 **전부** 직후 #7 스택이 0으로 리셋
- [x] 팬텀 미사용 사이클(t=15.733)에서 #7 스택 3 유지
- [x] #7 스택 분포 `{0: 218, 1: 337, 2: 319, 3: 1231}` — 상한 3

> ⚠️ **버스트 딜 값만으로는 #15(스택 제거)를 검증할 수 없다.** `max_stack: 3` 상한 때문에
> 제거가 아예 안 돼도 딜이 같다. 반드시 **버스트 직후 스택 값**을 직접 본다.
> 이때 버스트 프레임 자체의 `get_buffs` 샘플은 **버스트 딜 계산용(제거 전)** 이므로 제외해야 한다
> (`t > burst_t + 0.02`). 이걸 놓치면 정상 동작을 FAIL로 오판한다.

**자버프**
- [x] #10·#11 유지율 **100.0%** (t>1.0s 이후)
- [x] #14 최대 장탄 50% — 탄창별 발사 수 `80 / 89 / 63 / 59 / 76 / 109 / 59 / 79`(기본 최대 60발)
- [x] #13 받는 대미지 18% — 작열 보스에서 t=3.217부터 활성, 2064프레임

**네거티브 (발동하면 안 되는 것)**
- [x] 괴도의 단검 3중첩 미만일 때 #5·#7·#8·#9 무발동 (proc 42회 = 3중첩 도달 횟수와 일치)
- [x] #1 `accuracy_pct`가 `core_px = 0` 보스에서 DPS를 바꾸지 않는다 —
      명중률 값을 0으로 바꿔도 팬텀 딜 **736,297,480**으로 동일
- [x] 팬텀이 스쿼드 딜의 77%를 먹지 않는다 — **47.6%**(736,297,480 / 1,548,357,885)

**사이클**
- [x] 사이클 간격 **12.533s**(전 구간 단일값), 팬텀 버스트 8회 · 간격 **25.067s**
- [x] 배열 순서 4건(§스킬 간 상호작용 표)이 유지된다 — 순서가 바뀌면 조용히 무발동한다

**애장품 단계별 (2026-08-17 추가)**

위 항목은 전부 3단계 기준이다. 낮은 단계에서는 아래를 함께 본다.

- [x] 0~3단계 전부 `char_effects()`가 끊기지 않고 효과 11·12·13·15개를 조합
- [x] 이름 참조(#6·#9·#15의 `target_effect`, #5·#9의 `self_stack_above:괴도의 단검:3`,
      #7의 `stack_reach:괴도의 단검:3`)가 **전 단계에서 해소**된다 — 번호 4가 비어도 무관
- [x] **0단계 네거티브**: #5·#6·#7·#9 발동 0회. `괴도의 단검`이 1중첩을 못 넘는 것이 원인이며
      **의도된 결과**다(유저 확정) — 스택 공급 로직을 고쳐 3중첩을 만들지 않는다
- [x] 0단계 `괴도의 단검` 부여 71회 · 5초 주기 / 1단계 162회 — 차이가 #3의 공급분
- [x] #10·#11이 전 단계 **207회**로 동일 — 루프와 무관한 자버프는 판본 영향 없음
- [x] #12 `비기 괴도 난무` 전 단계 **8회**, 값 1457.28 동일
- [x] 단계별 스쿼드 총딜 단조 증가: 1,751.2M / 1,867.3M / 2,079.1M / 2,416.5M

**회귀**
- [x] `python -m context.snapshot` **20/20 PASS**
- [x] 마르차나 : 마린 스터디(하네스 미포함, `buff_stack_add` 주 사용처) 수동 A/B **diff 0**
