# 퀸(마코토) 동작 시나리오

> 모드: 보강
> 원문 출처: `scraper/nikke_scraped.json` (CDN 수집, 스킬 레벨 1~10)
> 파싱 결과: `data/parsed_skills.json` `["퀸(마코토)"]` — 15개 효과

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: 40s
- 무기 유형: SG (9발 / 재장전 1.50s / 공격력 201.5% · 코어 200% / 펠릿 10)
- 코드: 작열 → 우월 대상은 **풍압** 적
- 작성 사이클 수: 2 (쿨 40s)

**콜라보 공용 메카닉**: `1more`·`추격`·`페르소나 상태`는 [유키코](유키코.md)와
이 캐릭터에게만 있다. 두 시나리오를 함께 본다.

## 효과 요약 (보강 — 효과 단위 stat 키)

| ID (`name`) | 출처 | 트리거 | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|
| `페르소나 - 요한나` | 스킬1 | `battle_start` | `persona_state` (마커, 값 없음) | self | 영구 |
| `핵열 부스터` | 스킬1 | `battle_start` | `element_bonus_pct: 13.59` | self | 영구 |
| `방어의 마음가짐` | 스킬1 | `battle_start` | `def_pct: 14.78` | self | 영구 |
| `페르소나 : 요한나` | 스킬1 | `battle_start`, `full_burst_end` | `atk_pct: 50.28` | self | 15s |
| `페르소나 : 요한나 2` | 스킬1 | `event:1more` | `split_damage: 548.99` | all_enemies | — |
| `페르소나 : 요한나 3` | 스킬1 | `event:추격` | `split_damage: 548.99` | all_enemies | — |
| `철・권・제・재!` | 스킬2 | `battle_start` | `atk_dmg_pct: 30` | self | 영구 |
| `핵열 하이부스터` | 스킬2 | `burst_cast` | `element_bonus_pct: 25.56` | self | 영구(조건부 제거) |
| `라쿠카쟈` | 스킬2 | `burst_cast` | `def_pct: 17.95` | self | 영구(조건부 제거) |
| `철・권・제・재! 해제` / `해제 2` | 스킬2 | `full_burst_end` | `remove_named_buff` → `핵열 하이부스터` / `라쿠카쟈` | self | — |
| `철・권・제・재! 2` | 스킬2 | `burst_enter:3` | `split_dmg_pct: 90.01` | self | 10s |
| `배턴 터치` | 스킬2 | `event:1more` | `atk_caster_based_pct: 35.2` (`max_stack: 3`) | `allies_burst3_persona_excl_self` | 영구 |
| `마하프레이다인` | 스킬3 | `burst_cast` | `split_damage: 1421.69` | all_enemies | — |
| `1more` | 스킬3 | `burst_cast` + `target_code:풍압` | `atk_pct: 30.27` | self | 10s |

## 검증 스쿼드

- **표준**: `["리틀 머메이드", "크라운", "퀸(마코토)", "test_B3"]`
- **변형 1 (조건: Step3 페르소나 아군 존재)**: `["리틀 머메이드", "크라운", "퀸(마코토)", "유키코"]`
  — `배턴 터치` 대상 판정, 유키코 `추격` 수령 → `페르소나 : 요한나 3` 발동 검증.
  유키코 시나리오의 변형 1과 같은 스쿼드라 한 번의 실행으로 양쪽을 본다.
- **적 설정 (공통)**: `enemy={"code": "풍압"}` — 기본값 `code: None`에서 벗어난 설정.
  풍압이 아니면 `1more`이 붙지 않아 `페르소나 : 요한나 2`·`배턴 터치`가 전부 죽고,
  `핵열 부스터`·`핵열 하이부스터`(`element_bonus_pct`)도 factor⑦에서 무효다.
  **기본 layer 이탈이므로 시뮬 결과와 함께 이탈 목록을 보고한다.**

## 사이클 타임라인 (표준 스쿼드, 2사이클)

| t (예상) | 이벤트 | 발동 효과 (ID) | 대상/지속/스택 | 비고 |
|---|---|---|---|---|
| 0 | `battle_start` | `페르소나 - 요한나`, `핵열 부스터`, `방어의 마음가짐`, `철・권・제・재!`, `페르소나 : 요한나` | self / 영구·15s | `atk_pct`만 15s |
| 3.0 | B1 (리틀 머메이드) | — | — | `first_burst_time` |
| 3.1 | B2 (크라운) | — | — | |
| 3.2 | B3 = 본인 `burst_cast` | `핵열 하이부스터`, `라쿠카쟈`, `마하프레이다인`, `1more`(풍압 조건) | self / 영구·10s, 적 전체 | `burst_enter:3`도 이 시점 → `철・권・제・재! 2` |
| 3.2+ | `1more` 활성 → `event:1more` | `페르소나 : 요한나 2`(548.99 분배), `배턴 터치`(대상 없음) | 적 전체 | **부여 → 트리거 순서 전제** (유저 확인 Q5) |
| 13.25 | `full_burst_end` | `철・권・제・재! 해제`·`해제 2` 실행, `페르소나 : 요한나` 재부여 | self / 15s | 제거와 재부여가 같은 timing |
| 15.5~15.8 | 2사이클 버스트 (B3 = test_B3) | 본인 미사용 → 스킬2 버스트 블록·`1more` 없음 | — | 쿨 40s |
| 25.8 | `full_burst_end` | `페르소나 : 요한나` 재부여 | self / 15s | |
| 28.3~ | 3사이클 = 본인 버스트 재사용 | 1사이클과 동일, `배턴 터치` 2스택째(변형 1에서) | | 사이클 간격 12.55s |

변형 1에서는 유키코 버스트 사이클마다 `추격`을 받아 `페르소나 : 요한나 3`(548.99 분배)이 추가로 발동한다.

## 스킬 간 상호작용

- **`1more` → `페르소나 : 요한나 2` / `배턴 터치`**
  - 경로: `1more`(buff, name=`1more`) 활성 → `_activate()`가 `event:1more` 발생 → 두 효과 트리거
  - 시점: 같은 `burst_cast` 시점. 부여가 먼저 처리돼야 트리거가 산다 (유저 확인 Q5로 확정)
  - 위험: `event:{name}`은 **스쿼드 전체 브로드캐스트**([buff_manager.py:1880-1882](../../calculator/buff_manager.py#L1880-L1882)).
    변형 1에서 유키코가 `1more`을 얻을 때도 마코토의 `event:1more`가 발동해 **분배 대미지가 2배로 나온다.**
    `1more`은 본인 한정, `추격`은 수령자 기준이어야 하므로 단계 4에서 스코프를 분리해야 한다
- **유키코 `추격` → `페르소나 : 요한나 3`**
  - 경로: 유키코 `event:1more` → `추격`(`atk_caster_based_pct`, 25s)이 마코토에게 부여 → 수령 순간 `event:추격` → 548.99 분배
  - 시점: 유키코 버스트 사이클 종속. 마코토가 버스트를 쓰지 않는 사이클에도 발동한다
- **`배턴 터치` ↔ 유키코**: 마코토의 `1more`마다 유키코에게 1스택(`atk_caster_based_pct` 35.2, 최대 3).
  본인 버스트 3회 = 약 75초가 지나야 풀중첩이다
- **`철・권・제・재! 2`(`split_dmg_pct` 90.01) → 본인 분배 대미지 3종**: `마하프레이다인`(1421.69)과
  `페르소나 : 요한나 2·3`(548.99×2)이 모두 factor⑥에서 이 배수를 탄다 (유저 확인 Q6)

## 유저 확인 사항

> 아래 답변은 2026-08-12 유저 확인. **GAMEPLAY 승격은 유저가 명시적으로 승인하지 않았으므로 하지 않는다**
> — 이 두 캐릭터에 한정된 규칙으로 다룬다.

- Q1. `1more`의 획득 경로가 **자기 버스트의 "풍압 코드 적이 있다면"** 뿐인가?
  A. **이 둘(마코토·유키코)에게만 있는 메카닉**이다. 다른 획득 경로 없음.
- Q2. "1more 적용 시"는 `[1more]` 버프가 붙는 순간 1회 트리거인가?
  A. **붙는 순간 1회.** 유지 시간 동안 재발동하지 않는다.
- Q3. `[추격]`도 같은 구조인가 — 유키코가 부여하는 `[추격]`이 붙는 순간이 "추격 적용 시"인가?
  A. **맞다.**
- Q4. **페르소나 상태** = 스킬1 `[페르소나 - X]` 보유 상태인가?
  A. **맞다.** → `persona_state` 마커 stat으로 구현.
- Q5. 동일 timing 실행 순서: ① 버스트 분배 대미지 ② `[1more]` 부여 ③ 1more 트리거 2건?
  A. **맞다.**
- Q6. "분배 대미지 90.01%▲"가 버스트 1421.69% + 스킬1 548.99%×2 전부에 걸리나?
  A. **맞다** (본인의 분배 대미지 계수 전부).
- Q7. 스킬1의 1more·추격 분배 대미지가 둘 다 548.99%로 같고, 동시 충족 시 각각 발동하나?
  A. **맞다. 각각 1회씩 발동한다** (동시 충족 시 2회).
- Q8. `[배턴 터치]` 3중첩의 누적 조건은?
  A. **1more 적용마다 1스택.** 풀중첩(3스택)까지 **본인 버스트 3회**가 필요하다.
- Q9. SG 펠릿 수·연사속도
  A. **일반 SG 무기군 기본값을 따르고, 펠릿은 10개.**
  → 별도 고정 없이 무기군 기본값(SG 펠릿 10)으로 해결. 원래는 `parse_nikke.py`가 펠릿 미상 시 1을
  써넣어 그 값이 무기군 기본값을 덮었는데, **값이 없으면 키를 만들지 않도록 파서를 고쳤다**
  (`scraper/parse_nikke.py`). CDN 수집분은 전원 펠릿·총구가 있어 기존 캐릭터에는 영향이 없다.
  → 출시분 CDN 실값이 무기군 기본값과 동일함을 확인(펠릿 10 · 1.5발/s · 총구 1).
- Q10. `[배턴 터치]` 3중첩의 유지·초기화 시점은? (2026-08-13 확인)
  A. **초기화 없다.** 1more마다 1스택이 쌓이고 전투가 끝날 때까지 유지된다 → `duration: -1`.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| `1more` | 버프 이름 자체가 `1more`이고, 활성되는 순간 `event:1more`가 발생한다 | 원문 `[1more : 공격력 {0}% ▲]` + 유저 Q2 | 별도 시스템 상태였다면 부여 경로·횟수가 달라져 분배 대미지 발동 수가 바뀜 |
| `1more` 조건 | `풍압 코드 적이 있다면` = `target_code:풍압` (단일 보스라 동치) | `PARSING.md §4-2` 신규 매핑 | 복수 적 환경이면 "존재 여부"와 "대상 코드"가 갈리지만 sim 범위 밖 |
| `페르소나 : 요한나 2` / `3` | 서로 다른 트리거의 **독립 2항목**, 각각 1회 발동 | 원문의 별개 `■` clause + 유저 Q7 | 하나의 효과였다면 동시 충족 시 딜 절반 |
| `배턴 터치` | `atk_caster_based_pct`(시전자 기준) 3중첩, 1more마다 +1 | 원문 `시전자 기준 공격력` + 유저 Q8 | 즉시 3중첩이었다면 초기 사이클 유키코 딜이 크게 높아짐 |
| `핵열 하이부스터` / `라쿠카쟈` | `duration: -1` + `full_burst_end`의 `remove_named_buff` 2건 | 원문 `제거 시점 : 풀 버스트 타임 종료 시` (`PARSING.md §7-7`) | `duration: 10`으로 뒀다면 풀버스트 연장 시 조기 소멸 |
| `철・권・제・재! 2` | `split_dmg_pct` — 본인 분배 대미지 전부에 적용 (factor⑥) | 유저 Q6 | 버스트 대미지에만 걸렸다면 스킬1 분배 딜이 크게 감소 |
| `마하프레이다인` | `split_damage` (버스트 스킬 대미지 아님) | 원문에 "버스트 스킬 대미지" 문구 없음 → `PARSING.md §6` 규칙 | `burst_damage`였다면 `burst_dmg_pct` 버프를 타서 딜 상승 |

## impl/bug-fix용 검증 체크리스트

검증 실행: 2026-08-12, 180초 · `enemy={"code":"풍압"}` · seed 기본.
2026-08-13 정식 등록(CDN 표기·레벨 1~9 반영) 후 재실행 — 발동 횟수·총딜 동일.

- [x] 풀버스트 진입 시 활성 버프(본인 버스트 사이클): `핵열 부스터`, `방어의 마음가짐`, `철・권・제・재!`, `페르소나 : 요한나`, `핵열 하이부스터`, `라쿠카쟈`, `철・권・제・재! 2`, `1more` — 버프 스냅샷에서 전부 확인 (PASS)
- [x] `핵열 하이부스터`·`라쿠카쟈`가 `full_burst_end`에 제거 — activate 8 / expire 7 (마지막 1건은 전투 종료 시점, PASS)
- [x] `페르소나 : 요한나`(`atk_pct` 50.28)가 t=0 + 매 `full_burst_end`에 재부여 (PASS)
- [x] 발동 횟수: 본인 `burst_cast` 8회(풀버스트 15회 중 격사이클), `페르소나 : 요한나 2` 8회 = 본인 1more당 1회 (PASS)
- [x] **네거티브**: 동반 편성에서 유키코의 `1more`로 `페르소나 : 요한나 2`가 발동하지 않음 — 마코토 8회 / 유키코 7회로 분리 (`event_scope: "recipients"` 적용, PASS)
- [x] **네거티브**: `enemy.code = 작열`(불일치)이면 `1more`·`페르소나 : 요한나 2`·`배턴 터치` 전부 0회, `마하프레이다인`만 8회 (PASS)
- [x] **네거티브**: 표준 스쿼드에서 `배턴 터치` activate 0회 (PASS)
- [x] 상호작용: 변형 1에서 `추격` 수령 → `페르소나 : 요한나 3` 7회(유키코 버스트 횟수와 일치), `배턴 터치`가 유키코에게 35.2 → 70.4 → 105.6(3중첩 상한)으로 누적 (PASS)
- [x] `element_bonus_pct` 합(13.59 + 25.56)이 factor⑦에 실림 — 풍압/작열 적 딜 차이로 확인 (PASS)
- [x] 사이클 간격: 12.53s (표준 프로필 범위, PASS)
