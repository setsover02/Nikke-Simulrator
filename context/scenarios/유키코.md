# 유키코 동작 시나리오

> 모드: 보강
> 원문 출처: `scraper/nikke_scraped.json` (CDN 수집, 스킬 레벨 1~10)
> 파싱 결과: `data/parsed_skills.json` `["유키코"]` — 14개 효과

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: 40s
- 무기 유형: MG (300발 / 재장전 2.50s / 공격력 5.57% · 코어 200%) — 예열은 [MG 예열](MG 예열.md)
- 코드: 작열 → 우월 대상은 **풍압** 적
- 작성 사이클 수: 2 (쿨 40s)

**콜라보 공용 메카닉**: `1more`·`추격`·`페르소나 상태`는 [퀸(마코토)](퀸(마코토).md)와
이 캐릭터에게만 있다. 두 시나리오를 함께 본다.

## 효과 요약 (보강 — 효과 단위 stat 키)

| ID (`name`) | 출처 | 트리거 | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|
| `페르소나 - 코노하나사쿠야` | 스킬1 | `battle_start` | `persona_state` (마커, 값 없음) | self | 영구 |
| `메디아` | 스킬1 | `battle_start` + `tick_interval: 3.0` + `self_state:페르소나 - 코노하나사쿠야` | `heal_hp_pct: 5.7` (instant) | all_allies | 영구 |
| `페르소나 : 코노하나사쿠야` | 스킬1 | `battle_start`, `full_burst_end` | `atk_pct: 65.37` | self | 15s |
| `페르소나 : 코노하나사쿠야 2` | 스킬1 | `event:1more` | `split_damage: 400.31` | all_enemies | — |
| `진홍의 꽃` | 스킬2 | `battle_start` | `atk_dmg_pct: 55.31` | self | 영구 |
| `화염 부스터` | 스킬2 | `burst_cast` | `split_dmg_pct: 90.01` | self | 영구(조건부 제거) |
| `진홍의 수호` | 스킬2 | `burst_cast` + `target_code:수냉` | `received_dmg_pct: -17.95` | self | 영구(조건부 제거) |
| `메디라마` | 스킬2 | `burst_cast` + `tick_interval: 3.0` + `self_state:화염 부스터` | `heal_hp_pct: 5.7` (instant) | all_allies | 영구(조건부 중단) |
| `진홍의 꽃 해제` / `해제 2` | 스킬2 | `full_burst_end` | `remove_named_buff` → `화염 부스터` / `진홍의 수호` | self | — |
| `진홍의 꽃 2` | 스킬2 | `burst_enter:3` | `element_bonus_pct: 48.15` | self | 10s |
| `추격` | 스킬2 | `event:1more` | `atk_caster_based_pct: 80.25` | `allies_burst3_persona_excl_self` | 25s |
| `마하라기다인` | 스킬3 | `burst_cast` | `split_damage: 1258.79` | all_enemies | — |
| `1more` | 스킬3 | `burst_cast` + `target_code:풍압` | `atk_pct: 45.33` | self | 10s |

`메디라마`는 `[진홍의 꽃]` 컨테이너 전체가 아니라 **`화염 부스터` 상태**를 조건으로 삼는다 —
컨테이너와 수명이 같으면서 이름 중복 없이 참조할 수 있는 유일한 하위 버프이기 때문이다.
따라서 `화염 부스터`가 `full_burst_end`에 제거되면 `메디라마`도 자동으로 멈춘다
(`duration: -1` 주기 instant는 매 틱 조건을 재평가한다).

주기의 기준점은 **버프 부여 시점**이다(유저 확인 Q10) — `every:3s`(전투 시작 기준)가 아니라
부여 트리거 + `tick_interval: 3.0`으로 등록한다. `메디아`는 `battle_start` 기준이라 t=3·6·9…로
결과가 같고, `메디라마`는 본인 버스트마다 재등록돼 버스트 +3·+6·+9초에 발동한다.

## 검증 스쿼드

- **표준**: `["리틀 머메이드", "크라운", "유키코", "test_B3"]`
- **변형 1 (조건: Step3 페르소나 아군 존재)**: `["리틀 머메이드", "크라운", "퀸(마코토)", "유키코"]`
  — `추격` 부여 대상 판정 검증. 마코토 시나리오의 변형 1과 같은 스쿼드.
- **적 설정 (공통)**: `enemy={"code": "풍압"}` — 기본값 `code: None`에서 벗어난 설정.
  풍압이 아니면 `1more`이 붙지 않아 `페르소나 : 코노하나사쿠야 2`·`추격`이 죽고,
  `진홍의 꽃 2`(`element_bonus_pct`)도 무효다. **이탈 목록을 결과와 함께 보고한다.**
  또한 이 설정에서는 `진홍의 수호`(수냉 조건)가 **항상 미발동**이다.

## 사이클 타임라인 (표준 스쿼드, 2사이클)

| t (예상) | 이벤트 | 발동 효과 (ID) | 대상/지속/스택 | 비고 |
|---|---|---|---|---|
| 0 | `battle_start` | `페르소나 - 코노하나사쿠야`, `진홍의 꽃`, `페르소나 : 코노하나사쿠야` | self / 영구·15s | |
| 3, 6, 9 … | `battle_start` 등록 후 3초 틱 | `메디아` | 아군 전체 회복 | 딜 기여 없음 |
| 3.0 / 3.1 | B1 / B2 | — | — | |
| 3.2 | B3 = 본인 `burst_cast` | `화염 부스터`, (`진홍의 수호`는 수냉 아니라 미발동), `마하라기다인`, `1more` | self / 영구·10s, 적 전체 | `burst_enter:3` → `진홍의 꽃 2` |
| 3.2+ | `1more` 활성 → `event:1more` | `페르소나 : 코노하나사쿠야 2`(400.31 분배), `추격`(대상 없음) | 적 전체 | 부여 → 트리거 순서 전제 |
| 6.2 / 9.2 / 12.2 | `화염 부스터` 유지 중 | `메디라마`가 `메디아`와 **별도로** 발동 (버스트 시각 +3초 간격) | 아군 전체 | 유저 확인 Q5·Q10 |
| 13.25 | `full_burst_end` | `진홍의 꽃 해제`·`해제 2`, `페르소나 : 코노하나사쿠야` 재부여 | self / 15s | `메디라마` 중단 |
| 15.5~15.8 | 2사이클 버스트 (B3 = test_B3) | 본인 미사용 → 스킬2 버스트 블록·`1more` 없음 | — | 변형 1에서는 `추격` 25s가 이 사이클까지 남음 |
| 25.8 | `full_burst_end` | `페르소나 : 코노하나사쿠야` 재부여 | self / 15s | |
| 28.3~ | 3사이클 = 본인 버스트 재사용 | 1사이클과 동일 | | 사이클 간격 12.55s |

## 스킬 간 상호작용

- **`1more` → `페르소나 : 코노하나사쿠야 2` / `추격`**
  - 경로: `1more` 활성 → `event:1more` → 두 효과 트리거
  - 위험: `event:{name}`이 **스쿼드 전체 브로드캐스트**([buff_manager.py:1880-1882](../../calculator/buff_manager.py#L1880-L1882)).
    변형 1에서 마코토가 `1more`을 얻을 때도 유키코의 `event:1more`가 발동해 400.31 분배와 `추격` 부여가
    한 사이클에 2번 일어난다. 단계 4에서 **`1more`은 본인 한정, `추격`은 수령자 기준**으로 스코프를 나눠야 한다
- **`추격` → 마코토 `페르소나 : 요한나 3`**
  - 경로: `추격`(`atk_caster_based_pct` 80.25, 25s)이 마코토에게 부여 → 수령 순간 `event:추격` → 548.99 분배
  - 시점: 25초 지속이라 마코토가 버스트를 쓰지 않는 사이클까지 걸친다
- **마코토 `배턴 터치` → 유키코**: 마코토 `1more`마다 `atk_caster_based_pct` 35.2 1스택(최대 3).
  유키코의 일반 공격·분배 대미지 전부를 스케일링한다
- **`화염 부스터`(`split_dmg_pct` 90.01) → `마하라기다인` + `페르소나 : 코노하나사쿠야 2`**: 둘 다 factor⑥에서 이 배수를 탄다
- **`메디아` ↔ `메디라마`**: 별개 instant 2건. 기준점이 달라(전투 시작 / 본인 버스트) 위상이 어긋나며,
  `화염 부스터` 유지 구간에서는 3초마다 각자 한 번씩 돈다

## 유저 확인 사항

> 아래 답변은 2026-08-12 유저 확인. **GAMEPLAY 승격은 유저가 명시적으로 승인하지 않았으므로 하지 않는다**
> — 이 두 캐릭터에 한정된 규칙으로 다룬다.

- Q1. `1more` 획득 경로가 자기 버스트의 "풍압 코드 적이 있다면" 뿐인가?
  A. **이 둘(마코토·유키코)에게만 있는 메카닉**이다. 다른 획득 경로 없음.
- Q2. "1more 적용 시" 트리거는 `[1more]`이 붙는 순간 1회인가?
  A. **붙는 순간 1회.**
- Q3. `[추격]`은 유키코가 부여하는 버프이고, 마코토의 "추격 적용 시"가 그 수령 순간인가?
  A. **맞다.**
- Q4. **페르소나 상태** = 스킬1 `[페르소나 - X]` 보유 상태인가?
  A. **맞다.** → `persona_state` 마커 stat으로 구현.
- Q5. `[메디아]`와 `[메디라마]`가 동시에 도는가, 대체인가?
  A. **각각 동시에 돈다** (별개 회복원).
- Q6. 스킬2 효과3 `수냉 코드 적으로부터 받는 대미지 17.95%▼` — 적 코드를 풍압으로 두면 항상 무효인데 그대로 두나?
  A. (미답 — 텍스트대로 `received_dmg_pct` + `target_code:수냉`으로 등록. 검증 스쿼드에서는 자연히 미발동)
- Q7. "분배 대미지 90.01%▲"가 버스트 1258.79%와 스킬1 400.31% 양쪽에 다 걸리나?
  A. **맞다.**
- Q8. `[추격: 시전자 기준 공격력 80.25%▲]`가 캐스터 기준 버프인가?
  A. **맞다** (2026-08-13 확인). 유키코 최종 ATK 기준 → `atk_caster_based_pct` 유지.
- Q9. MG 연사속도(초기·최대·예열 발수)
  A. **일반 MG 무기군 기본값을 따른다** (예열 곡선 포함).
  → 출시분 CDN 실값이 무기군 기본값과 동일함을 확인(1→70발/s, 예열 41.4발).
- Q10. 3초 주기 회복의 기준점은 전투 시작인가, 버프 부여 시점인가? (2026-08-13 확인)
  A. **버프 부여 시점 기준.** → `every:3s`를 버리고 부여 트리거 + `tick_interval: 3.0`으로 바꿨다.
  `메디아`는 `battle_start` 기준이라 결과가 같고, `메디라마`는 본인 버스트 시각 기준으로 옮겨간다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| `1more` | 버프 이름 자체가 `1more`, 활성 순간 `event:1more` 발생 | 원문 `[1more : 공격력 {0}% ▲]` + 유저 Q2 | 별도 시스템 상태였다면 발동 횟수가 달라짐 |
| `추격` | 유키코가 **부여**하고, 수령자에게 `event:추격`이 발생해 마코토 딜을 연다 | 원문 + 유저 Q3 | 자체 딜 효과였다면 마코토 548.99 분배가 영영 미발동 |
| `추격` 값 | `atk_caster_based_pct` — 유키코 최종 ATK 기준으로 환산해 수령자 `atk_flat`에 합산 | 원문 `시전자 기준 공격력` | 수령자 기준이면 마코토 ATK에 비례해 결과가 바뀜 |
| `화염 부스터` | `split_dmg_pct` — 본인 분배 대미지 전부 (factor⑥) | 유저 Q7 | 버스트 한정이면 스킬1 분배 딜 감소 |
| `화염 부스터` / `진홍의 수호` | `duration: -1` + `full_burst_end`의 `remove_named_buff` | 원문 `제거 시점 : 풀 버스트 타임 종료 시` | `duration: 10`이면 풀버스트 연장 시 조기 소멸 |
| `메디라마` 조건 | `self_state:화염 부스터` (컨테이너 대리) | 컨테이너 이름이 스킬 키와 중복돼 별도 마커를 두지 않음 | 컨테이너 마커를 따로 뒀다면 이름 충돌 회피 로직이 필요 |
| `메디아`·`메디라마` 주기 | 부여 트리거 + `tick_interval: 3.0` (부여 시점 기준) | 유저 Q10 | `every:3s`(전투 시작 기준)면 `메디라마` 위상·횟수가 달라짐 (딜 무관) |
| `마하라기다인` | `split_damage` (버스트 스킬 대미지 아님) | 원문에 "버스트 스킬 대미지" 문구 없음 | `burst_damage`였다면 `burst_dmg_pct`를 타서 딜 상승 |
| `진홍의 수호` | `received_dmg_pct: -17.95` + `target_code:수냉` | 원문 `수냉 코드 적으로부터 받는 대미지 ▼` | 무조건 감소였다면 생존 판정만 달라짐(딜 무관) |

## impl/bug-fix용 검증 체크리스트

검증 실행: 2026-08-12, 180초 · `enemy={"code":"풍압"}` · seed 기본.
2026-08-13 정식 등록(CDN 표기·레벨 1~9·Q10 주기 기준점 반영) 후 재실행 — 총딜 동일,
`메디라마`만 위상이 바뀌어 23 → 21회(회복이라 딜 무관).

- [x] 풀버스트 진입 시 활성 버프(본인 버스트 사이클): `진홍의 꽃`, `페르소나 : 코노하나사쿠야`, `화염 부스터`, `진홍의 꽃 2`, `1more` — 버프 스냅샷 확인 (PASS)
- [x] `화염 부스터`가 `full_burst_end`에 제거 — activate 7 / expire 7 (PASS). `진홍의 수호`는 수냉 적이 아니라 미발동 (아래 네거티브)
- [x] `페르소나 : 코노하나사쿠야`(`atk_pct` 65.37)가 t=0 + 매 `full_burst_end`에 재부여 (PASS)
- [x] `메디아` 59회(180/3초 ≈ 60), `메디라마` 21회 — `화염 부스터` 유지 구간(버스트 +3·+6·+9초)에서만 발동, `메디아`와 각자 돈다 (2026-08-13 재검증, PASS)
- [x] 발동 횟수: 본인 `burst_cast` 8회(표준)/7회(변형1), `페르소나 : 코노하나사쿠야 2`가 그와 동수 = 본인 1more당 1회 (PASS)
- [x] **네거티브**: 변형 1에서 마코토의 `1more`로 유키코 효과가 발동하지 않음 (`event_scope: "recipients"`, PASS)
- [x] **네거티브**: 코드 불일치 시 `1more`·400.31 분배·`추격` 부여가 전부 0회 (PASS)
- [x] **네거티브**: 표준 스쿼드에서 `추격` activate 0회 (PASS)
- [x] **네거티브**: 풍압 적 스쿼드에서 `진홍의 수호` activate 0회 (PASS)
- [x] 상호작용: 변형 1에서 `추격` 7회 부여 → 마코토 `페르소나 : 요한나 3` 7회, `배턴 터치`가 유키코에게 3중첩까지 누적 (PASS)
- [x] 사이클 간격: 12.53s (PASS)
