# 밀크 : 블루밍 바니 동작 시나리오

> 모드: 보강

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: 40s
- 무기 유형: SR (최대 장탄 6, 재장전 2.0s, 연사 1.0/s, 차지 1.0s, 풀차지 배율 250%, 코어 200%)
- 속성/클래스: 철갑 / 화력형 (테트라, 카페 스위티)
- 작성 사이클 수: 2 (버스트 사용 사이클 / 미사용 사이클)

## 효과 요약 (파싱 결과 — 배열 순서 그대로)

수치는 스킬 레벨 10 기준.

| # | ID (`name`) | 출처 | 트리거 | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|---|
| 1 | `부끄러움 참기` | 스킬1 | `full_charge_hit` | `pierce_enabled` (관통 특화) | self | 6.0s |
| 2 | `부끄러움 2` | 스킬1 | **H** | `split_damage` 290% | all_enemies | — |
| 3 | `부끄러움 3` | 스킬1 | **H** | `ammo_charge_pct` −100 (탄환 전소) | self | — |
| 4 | `부끄러움 4` | 스킬1 | **H** | `reload_time_fixed` 3.0 | self | −1 (§8이 해제) |
| 5 | `부끄러움 5` | 스킬1 | **H** | `force_reload` | self | — |
| 6 | `부끄러움 6` | 스킬1 | **H** | `atk_pct` +118.7 | self | 40.0s |
| 7 | **`부끄러움`** (상태 마커) | 스킬1 | **H** | (stat 없음) | self | **−1 (영구)** |
| 8 | `부끄러움 재장전고정 해제` | 스킬1 | `event:full_reload` | `remove_named_buff` → `부끄러움 4` | self | — |
| 9 | `성질 내기` | 스킬2 | `passive` + `self_state:부끄러움` | `pierce_dmg_pct` +64.7 | self | −1 |
| 10 | `성질 내기 2` | 스킬2 | `every:2s` + `self_state:구경났냐 짜식들아!!` | `split_damage` 447.7% | all_enemies | — |
| 11 | **`구경났냐 짜식들아!!`** (상태 마커) | 스킬3 | `burst_cast` | `debuff_immune:부끄러움` | self | 10.0s |
| 12 | `구경났냐 짜식들아!! 2` | 스킬3 | `burst_cast` | `pierce_dmg_pct` +117.64 | self | 10.0s |
| 13 | `구경났냐 짜식들아!! 3` | 스킬3 | `burst_cast` | `atk_pct` +220 | self | 10.0s |
| 14 | `구경났냐 짜식들아!! 4` | 스킬3 | `burst_cast` | `remove_named_buff` → `부끄러움` | self | — |
| 15 | `구경났냐 짜식들아!! 5` | 스킬3 | `burst_cast` | `remove_named_buff` → `부끄러움 6` | self | — |

**H** = `charge_hold:0.5` + condition `not_self_state:부끄러움` + `not_self_state:구경났냐 짜식들아!!`

배열 순서 제약: **7번(마커)은 반드시 2~6번 뒤**. 앞에 오면 같은 프레임에 2~6번의
`not_self_state:부끄러움`이 깨져 전부 무발동이 된다.

## 컨트롤 모델 (이 캐릭터의 핵심)

밀크는 **컨트롤이 곧 스킬 가동**이다. 컨트롤이 없으면 항목 2~7·9가 통째로 죽고
버스트 주기 분배딜(10번)만 남는다.

### 판정 산술

```
차지 시작 → +1.0s 풀차지 도달 → +0.5s 유지 → charge_hold:0.5 판정 (1회)
                                              └ 이 순간 면역이면 부끄러움 없음
```

- **판정은 풀차지 +0.5초 그 순간 1회뿐이다.** 계속 들고 있어도 재판정하지 않는다
  → 버스트 중에 홀드를 시작하면, **버스트가 끝난 뒤에도 부끄러움이 걸리지 않는다**(유저 확인).
- 반대로 **버스트 종료 1.3초 전에 차지를 시작**하면 판정 시각은 종료 +0.2초라
  면역이 이미 풀려 **부끄러움이 발동한다**. 차지 시작 시점이 1.5초 창을 어디에 놓느냐의 문제다.

### 실전 조작 (유저 서술)

| 구간 | 조작 | 이유 |
|---|---|---|
| 평시(비버스트) | **톡톡이** | 논차지 연사로 발수를 벌고, 그 사이 부끄러움 재장전이 끼기도 한다 |
| 풀버스트 중 | 톡톡이 + **가끔 풀차지 1발** | `관통 특화`(6초)를 켜 두어야 `pierce_dmg_pct`가 실린다. 6초 안에 최소 1발 |
| 버스트 종료 직전 | **종료 1.3초 전부터 차지** | 판정이 면역 해제 직후에 떨어져 부끄러움 발동 |
| 비버스트 / 타 B3 버스트 사이클 | 아무 때나 차지해 부끄러움 발동 | 면역이 없으므로 판정이 그냥 통과 |

**부끄러움을 켜는 대가**: 그 풀차지 샷은 **취소**되고(발사 없음), 탄창 전소 + 3.0초 재장전.
얻는 것은 `atk_pct` +118.7%(40초) + `pierce_dmg_pct` +64.7%(부끄러움 유지되는 한 영구).

### 계산기 표현 (구현 완료)

| 필요한 것 | 구현 |
|---|---|
| `charge_hold:N` 트리거 발동 | `CharState._notify_charge_hold()` — `_charge_full_t`로 유지 시간 측정, **차지당 1회** 판정 |
| 버스트 종료 직후에 판정 떨어뜨리기 | 홀드 정책 **`charge_hold_after_fb`** — `판정 = 풀버스트 종료 + lead`, 차지 시작을 역산. 의도한 차지가 시작될 때만 홀드를 건다 |
| 톡톡이 중 `관통 특화` 유지 | **`tap_fire.full_charge_interval`** — N초마다 한 발을 풀차지로. 밀크는 6초 버프라 **5.5** |

```bash
python -m context.sim "리틀 머메이드,크라운,밀크 : 블루밍 바니,test_B3" --seed 42 \
  --tap "밀크 : 블루밍 바니:4.0:0.03:5.5" \
  --hold-ctrl "밀크 : 블루밍 바니:charge_hold_after_fb:0.1"
```

**남은 한계**: 풀차지 시점이 **고정 간격**이다. 유저 실전 패턴("본인 버스트 6초 남았을 때 /
부끄러움 종료 직후 풀차지 → 4초 톡톡이 → 다시 풀차지")처럼 사이클 이벤트에 시점을 붙이는
표현은 아직 없다 (`CONTROL.md §미구현·보류`).

### 측정 (표준 스쿼드 180초 seed 42, 밀크 딜)

| 조작 | 밀크 딜 |
|---|---|
| 없음 | 382.8M |
| 부끄러움 홀드컨만 | 407.7M (+6.5%) |
| 톡톡이 + 풀차지 5.5초 | 466.9M (+22.0%) |
| **톡톡이 + 풀차지 + 부끄러움 홀드컨** | **482.1M (+25.9%)** |

두 조작이 각각 다른 축을 든다 — 풀차지 섞기는 `관통 특화`를 살려 관통 대미지 버프를
딜에 싣고, 부끄러움 홀드컨은 **공격력 +118.7%**를 비버스트·타인 버스트 구간에 깐다.

`부끄러움 6`(ATK) 가동 확인 (seed 42, 풀버스트 진입 시 스냅샷):

| 시각 | 구간 | `구경났냐 짜식들아!! 3`(+220%) | `부끄러움 6`(+118.7%) |
|---|---|---|---|
| 3.25 · 28.32 · 53.38 · 78.45 | **본인** 버스트 | 활성 | **없음** (버스트가 지움) |
| 15.78 · 40.85 · 65.92 · 90.98 | 타인 버스트 | — | 활성 |

부끄러움은 본인 버스트마다 지워지고 **버스트 종료 직후 다시 걸린다** — 180초에 7회
(각 버스트 +10.2초). 자기 버스트 구간의 공격력은 버스트 자체 버프(+220%)가 맡는다.

## 검증 스쿼드

아군 조건(버스트 단계·무기·클래스·코드·배치) 텍스트 단서가 없다 — 전부 자기 상태 조건이라
스쿼드 변형은 불필요. 변형은 전부 **컨트롤** 축이다.

- **표준**: `["리틀 머메이드", "크라운", "밀크 : 블루밍 바니", "test_B3"]`
- **변형 1 (컨트롤 없음)**: 부끄러움 0회. 항목 2~7·9 전부 무발동인 하한 기준선
- **변형 2 (실전, 기준안)**: 톡톡이 + 버스트 종료 1.3초 전 차지 + 풀버스트 중 풀차지 1발
- **변형 3 (홀드만)**: 톡톡이 없이 버스트 종료 직전 홀드만 — 톡톡이 기여분 분리용
- **변형 4 (버스트 중 홀드, 네거티브)**: 버스트 중에 홀드 시작 → **부끄러움이 끝까지 안 걸려야** 정상

## 사이클 타임라인 (표준 스쿼드 · 변형 2, seed 42 실측 기준)

실측 사이클: 풀버스트 3.25~13.25 / 15.78~25.78 / 28.32~38.32 …
밀크 버스트는 쿨 40s에 버쿨감이 겹쳐 **2사이클에 1회**(t≈3.2 → 28.27).

| t | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| 0 | battle_start | 9번은 등록되나 `self_state:부끄러움`이 거짓이라 **값 0** | 마커 없으면 관통 버프도 없다 |
| ~1.0 | 첫 풀차지 명중 | 1번 `pierce_enabled` 6s | |
| 3.20 | 밀크 burst_cast | 11·12·13 부여(10s) / 14·15 실행 | 이 시점엔 지울 부끄러움이 없다 |
| 3.25 | 풀버스트 시작 | 딜계수 +50% | |
| 5.2·7.2·9.2·11.2·13.2 | 10번 `성질 내기 2` | `split_damage` 447.7% ×5 | **사이클당 정확히 5회** (실측 확인) |
| ~11.95 | 차지 시작 (종료 1.3s 전) | — | 조작: 여기서 눌러야 판정이 면역 밖으로 나간다 |
| 13.20 | 11·12·13 만료 | 면역 해제 | |
| 13.25 | 풀버스트 종료 | | |
| ~13.45 | `charge_hold:0.5` 판정 | 2~7번 발동 — 분배딜 290% · 탄환 0 · 재장전 3.0s · `atk_pct` +118.7%(40s) · 마커(영구) | **풀차지 샷은 취소** → 1번 갱신 없음 |
| ~13.45 | 9번 조건 성립 | `pierce_dmg_pct` +64.7% (영구) | 단 1번이 만료돼 있으면 관통 판정이 아니라 **딜에 안 실린다** |
| ~16.45 | 재장전 완료 | 8번이 `부끄러움 4` 해제 → 이후 재장전 2.0s | `event:full_reload` |
| 15.78~25.78 | test_B3 사이클 | 밀크 버스트 없음 → 10번 0회, 11~13 없음 | 부끄러움·ATK는 유지 |
| 28.27 | 밀크 burst_cast (2회차) | 11·12·13 부여 + **14·15가 부끄러움·ATK 제거** | 여기서 9번도 함께 꺼진다 |
| 28.32~38.32 | 풀버스트 | 10번 5회 | 관통은 12번(+117.64%)이 담당 |
| ~37.0 | 차지 시작 | | 다음 사이클 부끄러움 재획득 |
| 53.45 | (재획득 안 했다면) `부끄러움 6` 만료 | ATK +118.7% 소멸, 마커·9번은 유지 | 마커가 영구라 **재발동도 막힌다** — 40초 뒤 ATK만 사라진 채로 남는 구조 |

## 스킬 간 상호작용

- **1번 `pierce_enabled` → 9·12번 `pierce_dmg_pct`**
  - 경로: `pierce_enabled`가 켜져야 일반 공격 hit이 `is_pierce_damage`로 판정되고
    (`timeline.py:501,625`) 그때만 `pierce_dmg_pct`가 ②에 가산된다 (`damage.py:201`).
  - 위험: **톡톡이(논차지)는 1번을 갱신하지 않는다.** 풀버스트 6초를 넘기기 전에 풀차지 1발을
    섞지 않으면 관통 버프 182.3%(9+12)가 통째로 죽는다. 유저가 "한 번씩 풀차지샷"이라고 한 이유.
  - 부끄러움 발동 프레임의 샷은 취소되므로 그 발로는 1번이 갱신되지 않는다. 이어지는
    3.0초 재장전 동안 6초 타이머가 계속 흐른다 — 재장전 후 첫 발까지 끊길 수 있다.

- **7번 마커 → 9번 `pierce_dmg_pct` (런타임 재평가)**
  - `self_state:`는 `_RUNTIME_COND_PREFIXES`라 마커가 사라지는 프레임에 9번도 즉시 0이 된다.
    14번(마커 제거)이 곧 9번 해제다.

- **11번 면역 → 2~7번 차단, 14·15번 → 기존 상태 해제**
  - 차단은 condition(`not_self_state:구경났냐 짜식들아!!`), 해제는 `remove_named_buff` 2개.
    `debuff_immune:[name]` 코드 경로는 harmful 버프만 걸러서 neutral 마커를 막지 못한다.
  - 결과: 버스트 진입 순간 밀크는 **+118.7%/+64.7%를 잃고 +220%/+117.64%를 얻는다**(교체).

- **7번 영구 마커 → 2~6번 재발동 봉쇄**
  - `not_self_state:부끄러움`이라 마커가 살아 있는 한 다시 걸 수 없다. ATK(40초)만 먼저
    만료되는 구간이 생긴다. **버스트로 마커를 지우는 것이 유일한 리셋**이다.

- **3·4·5번의 배열 순서**
  - `reload_time_fixed`(4)가 `force_reload`(5)보다 **앞**이어야 그 재장전이 3.0초가 된다.
    `_start_reload`가 진입 시점에 `_fixed_reload_time()`을 읽기 때문.

## 유저 확인 사항

- Q1: `부끄러움` 지속 시간 → **영구.** 버스트 `부끄러움 면역`으로만 사라진다.
- Q2: 부끄러움 발동 시 풀차지 샷 → **취소된다.** 그 발로 `관통 특화`도 부여되지 않는다.
- Q3: 실전 운용 → 아래 Q9로 확장 확정.
- Q4: `atk_pct` 40초 → **갱신된다.** 실전에선 버스트로 지워진 뒤 다시 켜는 운용.
- Q5: 면역 → **기존 부끄러움을 해제한다.**
- Q6: 버스트 10초 주기 분배딜 → **5회.** 마지막 틱이 버스트 종료 순간에 들어간다.
- Q7: `재장전 속도 50% 감소 상태로 고정` → **3.0초**(기본 2.0 + 1.0), 다른 재장전 버프 무시.
- Q8: `[재장전 1회 적용]` → **그 1회만.** 다음 재장전부터 평시 속도.
- Q9: 컨트롤 상세 →
  - `charge_hold:0.5` **판정은 풀차지 +0.5초 1회뿐**. 버스트 중에 홀드를 시작하면 계속 들고
    있어도 부끄러움이 걸리지 않는다.
  - **버스트 종료 1.3초 전 차지 시작** → 판정 시각(1.5초 후)에 면역이 풀려 발동.
  - 평시는 **톡톡이**, 풀버스트 중에는 `관통 특화` 유지용 **풀차지 1발**을 섞는다.
  - 비버스트 구간은 오토 사격 또는 부끄러움 재장전 대기.
- Q10: 주기 공격(틱)의 첫 발동 시점 → **캐릭터마다 두 유형**이 있다.
  - **type 1 — 발동과 동시에 첫 틱**: 미하라 : 본딩 체인 · 에이다 · 아크레인저 블랙
  - **type 2 — 발동 후 interval부터**: 디젤 : 윈터 스위츠 · **밀크 : 블루밍 바니**
  - 회수는 양쪽 같다 (2초/10초 = 5회, 1초/10초 = 10회). 다른 것은 **위상**이다 —
    type 1은 첫 틱이 풀버스트 시작 전에 들어갈 수 있고, type 2는 첫 틱부터 풀버스트 안이다.
  - **계산기 수정 필요** (아래 §미해결).

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| `부끄러움`(7) | 영구 상태 마커. stat 없는 buff, `duration: -1` | Q1 | 유한 지속으로 두면 9번 관통 버프가 조기 종료 |
| `부끄러움`(7) 배열 위치 | 2~6번 **뒤**에 배치 | 같은 프레임 `not_self_state` 게이팅 | 앞에 두면 2~6번 전부 무발동 |
| `부끄러움 면역`(11) | 차단은 condition, 해제는 `remove_named_buff` 2개로 분해 | Q5 + `debuff_immune:[name]`이 harmful만 거름 | stat 하나로만 두면 기존 부끄러움이 버스트 내내 살아남아 관통 버프가 이중으로 실린다 |
| `부끄러움 4` | `reload_time_fixed: 3.0` (절대 고정) | Q7 — "고정"이 다른 버프 무시를 뜻함 | `reload_speed_pct: -50`이면 값은 같지만 타 재장전 버프에 흔들린다 |
| `성질 내기 2`(10) | `every:2s` + `self_state` condition | Q6 실측 5회 | `tick_interval`+`duration`이면 만료 틱이 빠져 4회 (−20%) |
| `부끄러움 2`(2) | 발동해도 **그 풀차지 샷은 없다** | Q2 | 샷이 나간다고 보면 발수·`관통 특화` 갱신이 사이클마다 1회씩 과다 계상 |
| `charge_hold:0.5` | 풀차지 +0.5초 **1회 판정**, 유지해도 재판정 없음 | Q9 | 매 프레임 재판정이면 버스트 중 홀드가 면역 해제 순간 발동해 버려, 유저가 쓰는 회피 조작이 사라진다 |

## 주기 틱 두 유형 (해결 — 별건으로 처리 완료)

`tick_start` 필드로 두 유형을 구분하고 만료 프레임 틱을 살렸다. 밀크의 `성질 내기 2`는
`every:2s`(전역 격자)에서 **`burst_cast` + `tick_interval: 2.0` + `duration: 10.0`**(type 2)으로
옮겨 발동 기준 위상으로 맞췄다 — 버스트 +2·4·6·8·10초에 **5회**, 마지막 틱도 버스트 버프를
받는다(4.13M, 고치기 전 1.47M). 규칙의 정본은 `GAMEPLAY.md §효과 실행 순서`,
영향 범위는 커밋 `dedbe2f`(주기 대미지 첫 틱 두 유형 — 기존 DoT 캐릭터 전반, 8스쿼드 baseline 갱신).

**에이다 `섬광 수류탄 투척`은 이 수정에 포함되지 않았다** — `every:2s` 경로라 위상이 여전히
전투 시작 기준 전역 격자다. 게다가 발동 간격을 2초 → 1초로 줄이는 `effect_interval`이
**미구현**이라(코드에 핸들러 없음) 인게임 10회 대신 5회만 나간다. 별도 과제다.

## impl/bug-fix용 검증 체크리스트

**선행 구현**
- [x] `charge_hold:N` notify 연결 (`_notify_charge_hold`, 차지당 1회 판정)
- [x] 홀드 정책 `charge_hold_after_fb` (버스트 종료 기준 판정 시각 역산)
- [x] 톡톡이 중 주기적 풀차지 `tap_fire.full_charge_interval`
- [ ] 구간별 풀차지 **시점 지정**(사이클 이벤트 앵커) — 미구현, 고정 간격이 근사

**변형 1 (컨트롤 없음)**
- [x] 부끄러움 진입 **0회**, 항목 2~7·9 전부 발동 0
- [x] `성질 내기 2`는 밀크 버스트 사이클마다 **5회** (t=+2·4·6·8·10)

**변형 2 (실전)**
- [x] 부끄러움 진입 시 5개 후속이 모두 관측 — 분배딜 1 · ammo 0 · 재장전 3.0s · ATK 등록 · 마커 등록
- [x] 부끄러움 발동 프레임에 **풀차지 샷이 나가지 않는다** (t=13.350 부끄러움 2만, 기본 공격 없음)
- [x] 강제 재장전이 **정확히 3.0s**(13.35 → 16.35), 그 다음 재장전은 2.0s (`부끄러움 4`가 `event:full_reload`에 해제)
- [x] 마커가 살아 있는 동안 부끄러움 **재진입 0회**
- [x] 버스트 진입 프레임에 마커·ATK 제거 (t=28.317 스냅샷에서 `부끄러움`·`부끄러움 6` 소멸 확인)
- [x] 풀버스트 구간에서 `pierce_enabled`가 끊기지 않는다 (풀차지 5.5초 간격 혼합 시 `부끄러움 참기` 상시 활성)

**변형 4 (네거티브)**
- [x] 버스트 중에 홀드를 시작하면 버스트 종료 후에도 부끄러움 **0회** (판정 1회 규칙으로 구조적 보장)

**사이클**
- [x] 사이클 간격 12.55s ± / 밀크 버스트는 2사이클에 1회 (t=3.2 → 28.27, seed 42)
- [x] 변형 2 총딜 > 변형 1 총딜 — **+25.9%**. 처음에는 −4.8%로 나왔는데, 예정 차지 시각이
      재장전과 겹치면 그 사이클 판정을 통째로 버리는 컨트롤 버그였다(180초에 7회 → 3회).
      떼기 시각을 이번 차지 기준으로 잡아 해결.
- [x] 부끄러움이 **본인 버스트마다** 재획득된다 (버스트 +10.2초, 180초에 7회)
