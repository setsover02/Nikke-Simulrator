# 누아르 동작 시나리오

> 모드: 보강

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: 40s
- 무기 유형: SG (기본 최대 장탄 9발, 재장전 0.23s, 90rpm = 1.5발/s, 펠릿 10 / 총구 1, `damage_coeff` 204.6 / 코어 200%)
  - **기본 스펙(장비·큐브·소장품 포함)에서 실효 최대 장탄은 21발**이다(장비 `max_ammo_pct`). 9발로 어림하면 안 된다.
- 클래스/코드/기업: 화력형 / 풍압 / 테트라
- 스쿼드: 777 (동료: 블랑 B2·60s, 루주 B1·20s — 둘 다 파싱 완료)
- 작성 사이클 수: 2 (쿨 40s → 표준 스쿼드에서 2사이클당 1회 시전)

## 효과 요약 (보강 · `parsed_skills.json` 효과 단위)

수치는 **스킬 레벨 10** 기준. `values`는 1~10 전부 파일에 있다. 배열 순서 = 원문 `■` 블록 순서.

| ID | 출처 | 트리거 | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|
| `럭키챰` | 스킬1 | `passive` + `self_hp_above:70` | `atk_caster_based_pct` 14.08 (buff, beneficial) | `all_allies` | `-1` (영구) |
| `래빗 트윈즈 B` | 스킬2 | `full_burst_start` | `max_ammo_flat` 5 (buff, beneficial) | `all_allies` | 10s |
| `래빗 트윈즈 B 2` | 스킬2 | `full_burst_start` | `ammo_charge_pct` 39.88 (instant) | `all_allies` | — |
| `피날레` | 스킬3 | `burst_cast` | `burst_damage` 351.64 (damage, AoE) | `all_enemies` | — |
| `피날레 2` | 스킬3 | `burst_cast` | `accuracy_pct` 13.93 (buff, beneficial) | `allies_weapon:SG` | 10s |
| `피날레 3` | 스킬3 | `burst_cast` | **`intercept_dmg_pct`** 23.23 (buff, beneficial) | `allies_weapon:SG` | 10s |
| `피날레 4` | 스킬3 | `burst_cast` + `squad_ally_exists` | `accuracy_pct` 11.61 (buff, beneficial) | `all_allies` | 30s |
| `피날레 5` | 스킬3 | `burst_cast` + `squad_ally_exists` | **`intercept_dmg_pct`** 19.36 (buff, beneficial) | `all_allies` | 30s |

구현 상태 (`IMPL-STATUS.md` 기준):
- ✅ `atk_caster_based_pct` · `max_ammo_flat` · `ammo_charge_pct` · `burst_damage`
  / timing `passive` · `full_burst_start` · `burst_cast` / condition `self_hp_above:N` · `squad_ally_exists`
  / target `all_allies` · `all_enemies` · `allies_weapon:SG`
- ⚠️ `accuracy_pct` — DealForm 어느 항에도 없고 코어 보유 적(`core_px > 0`)의 코어히트율로만 반영.
  기본 보스는 `core_px = 0`이라 **딜 기여 0**(네거티브 항목).
- 🚫 **`intercept_dmg_pct`** — **구현하지 않는다**(Q7 최종 결정). 적 모델에 저지 부위가 없어
  조건을 언제나 미달성으로 둔다 → 딜 기여 영구 0. 파싱만 등록하고 네거티브 항목으로 검증한다.

**딜 관점 요약**: 스쿼드 DPS 기여는 세 갈래다.
1. `럭키챰` — 아군 전원 `atk_flat` += (누아르 **기본** 공격력 × 14.08%). 전투 내내 상시.
2. `래빗 트윈즈 B`/`B 2` — 아군 전원의 사격 횟수를 늘린다. 풀버스트 구간 재장전 손실을 줄이는 게 본체.
3. `피날레` — 누아르 본인 딜의 일부(예비 실측 기준 본인 딜의 약 2.6%, 나머지는 SG 기본공격).

## 검증 스쿼드

아래 수치는 **단계 4 구현 후 실측**이다 — `--seed 1`, 180초, 기본 스펙,
장탄 두 건 수정 반영 · `intercept_dmg_pct` 미구현(🚫 확정).

- **표준 (동일 스쿼드 아군 없음 · SG는 본인뿐)**: `["리틀 머메이드", "크라운", "누아르", "test_B3"]`
  — `피날레 4·5` **무발동**(`squad_ally_exists` 거짓), `피날레 2·3` 대상은 누아르 1인.
  간격 12.533s 균일 · 풀버스트 **15회** · 누아르 버스트 **8회**(2사이클당 1회, 재사용 간격 25.067s)
  · 스쿼드 총딜 1,137.1M.
- **변형 1 (SG 아군 추가 — `피날레 2·3` 대상 확장)**: `["리틀 머메이드", "크라운", "누아르", "드레이크"]`
  — `test_B3` → 드레이크(SG·B3·40s). 사이클 구조는 표준과 동일(15회 / 누아르 8회 / 25.067s).
  드레이크 자체 버프로 총딜이 1,721.3M까지 오르므로 **총딜이 아니라 버프 부여 대상 수로 판정**한다.
- **변형 2 (777 동료 — `피날레 4·5` 발동)**: `["루주", "크라운", "누아르", "test_B3"]`
  — 리틀 머메이드(B1) → 루주(777·B1·20s·**SR**). 루주가 SR이라 `피날레 2·3`(SG) 대상을 오염시키지 않아
  ②③ 분리 관찰이 가능하다. 루주 버쿨감 7.0 프로필 → 간격 **13.0s**(중간 1회 12.25) · 풀버스트 **14회**
  · 누아르 버스트 **7회**(재사용 간격 26.0s, 1회 25.25s) · 총딜 684.8M.
- **(보류) 변형 3 (777 + 블랑)**: `["리틀 머메이드", "블랑", "누아르", "test_B3", "스노우 화이트 : 헤비암즈"]`
  — 블랑의 `래빗 트윈즈 W 2`(`squad_ally_exists`, −40.76s)가 누아르 때문에 켜져 **양쪽 사이클이 동시에 바뀐다.**
  단독 변인 관찰에 부적합 → 블랑 문서 갱신(Q8) 확인용으로만 쓴다.

## 사이클 타임라인

표준 스쿼드 기준. 시각은 위 조건의 예비 실측이다.

| t (실측) | 이벤트 | 발동 효과 | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0 | battle_start | `럭키챰` | all_allies · 영구 | `passive` — 스냅샷에 `(영구)`로 확인됨 |
| 3.000 | stage:1 리틀 머메이드 | — | — | |
| 3.100 | stage:2 크라운 | — | — | |
| 3.200 | stage:3 **누아르** `burst_cast` | `피날레` · `피날레 2` · `피날레 3` | 적 AoE 딜 / SG 아군 10s ×2 | 표준에선 `피날레 4·5` 무발동 |
| 3.250 | full_burst 시작 | `래빗 트윈즈 B` · `래빗 트윈즈 B 2` | all_allies 10s / 즉시 충전 | 잔탄 21 → **26**(상한이 26으로 올라간 뒤 충전) |
| 13.200 | — | `피날레 2·3` 만료 | — | |
| 13.250 | full_burst 종료 | `래빗 트윈즈 B` 만료 | — | 상한 26 → 21. 이 시점 누아르 잔탄은 11이라 캡이 물리지 않는다(사격이 빨라서) |
| 15.533 | stage:1 리틀 머메이드 | (리틀 머메이드 충전) | 누아르 잔탄 7 → 14 | |
| 15.783 | 2사이클 full_burst 시작 (누아르 **버스트 미사용**) | `래빗 트윈즈 B` · `B 2` | all_allies | 잔탄 14 → **24** (=26×39.88%≈10) |
| 25.783 | 2사이클 full_burst 종료 | — | — | |
| 28.267 | 3사이클 stage:3 **누아르** 2회차 | `피날레` 계열 재발동 | — | 재사용 간격 **25.07s** |

- `래빗 트윈즈 B`/`B 2`는 **매 사이클** 발동 → 표준 15회, 변형 2는 14회.
- `피날레` 계열은 누아르 본인 시전 사이클에만 → 표준 8회, 변형 2는 7회.
- **Q3(30초 지속 vs 쿨 40초) 해소**: 재사용 간격이 표준 25.07s · 변형 2 26.0s라
  `피날레 4·5`(30s)는 **끊기지 않고 이어진다**. 변형 2 스냅샷에서 `33.20s까지`로 확인.

## 스킬 간 상호작용

- **`럭키챰` → 아군 전원의 `atk_flat`**
  - 경로: `state["base_stats"]["누아르"]["atk"] × 14.08%`가 각 아군의 `atk_flat`에 가산
    ([buff_manager.py:2267](calculator/buff_manager.py#L2267)).
  - 시점 쟁점 없음 — **버프 제외 기본 공격력** 기준이라 부여 시점/재평가 구분이 결과를 바꾸지 않는다(Q2).
  - 단 `self_hp_above:70`은 `_RUNTIME_COND_PREFIXES`에 있어 **매 조회 시 재평가**된다.
    `max_hp_only_pct` 계열이 걸려 `hp_pct`가 70 밑으로 내려가면 **아군 전체 공격력 버프가 통째로 꺼진다**(Q1).
  - 위험: `atk_pct`로 오파싱하면 아군 각자의 공격력에 14.08%가 곱해져 딜이 크게 과대해진다.
- **`래빗 트윈즈 B`(장탄 +5) → `래빗 트윈즈 B 2`(충전 39.88%) 실행 순서**
  - 배열이 B → B 2 순서이고, 충전은 **증가된 최대치 기준**이어야 한다(Q4).
  - **구현 결함 1**: `_effective_max_ammo()`가 `max_ammo_pct`만 읽고 `max_ammo_flat`을 더하지 않는다
    ([timeline.py:1771-1773](calculator/timeline.py#L1771-L1773)). 재장전 경로의 `CharState._full_ammo()`는
    더한다([timeline.py:1239-1242](calculator/timeline.py#L1239-L1242)) — 두 경로가 어긋나 있다.
    실측에서 t=15.783 충전이 8발(=20×39.88%)이었다. 수정 후 기대치는 10발(=25×39.88%)이고 상한도 25다.
  - **구현 결함 2**: `래빗 트윈즈 B` 만료 시 초과 잔탄 캡이 없다(Q5). 잔탄은 발사로만 줄고 만료 재평가 경로가
    아예 없어, 최대치가 26 → 21로 돌아와도 22~26발을 그대로 쏜다 → 사격 횟수 과대.
  - 두 결함은 **반대 방향**이다(1은 과소, 2는 과대). 한쪽만 고치면 총딜이 한 방향으로 치우치므로 함께 고친다.
- **`피날레 2·3`(SG 10s)와 `피날레 4·5`(전체 30s)의 stat 중복**
  - 양쪽에 걸리는 아군(=SG를 든 777 동료)은 값을 **합산**받는다(Q6): 명중률 25.54 / 저지 부위 42.59.
  - 지속이 10s·30s로 달라 **별개 항목으로 유지**해야 한다. 합치면 만료 시각이 틀어진다.
- **`피날레`(AoE 버스트 딜)와 뒤따르는 버프들의 순서**
  - 원문 블록 순서가 딜 → 버프이고 뒤 블록이 전부 아군 버프라 딜에 영향이 없다 — 순서 쟁점 없음.
  - `all_enemies` + `burst_damage` 조합이므로 `burst_dmg_aoe_pct` 증폭 대상(`is_aoe_burst`)이 된다.

## 유저 확인 사항

- Q1. **`자신의 체력이 70% 이상이라면`** — 보스 sim은 아군 피격 모델이 없어 항상 100%이므로
  이 조건은 사실상 **항상 참**으로 봐도 되는가?
  **A: 아니다. 상수화하지 말고 조건으로 구현해야 한다** — `최대 체력만 증가`(`max_hp_only_pct`,
  현재 체력 유지) 효과가 걸리면 `hp_pct`가 70% 밑으로 내려가 **발동이 꺼질 수 있다.**
  → `self_hp_above:70`. 이미 `_RUNTIME_COND_PREFIXES`에 있어 런타임 재평가되므로 추가 구현은 불필요.
  **일반 규칙 → `GAMEPLAY.md §condition` 승격 완료.**
- Q2. **`시전자 기준 공격력 ▲`의 캐스터 값 산정 시점**
  **A: 시전자 기준 공격력은 버프를 제외한 기본 공격력 기준이므로 시점 문제가 없다.**
  구현도 `state["base_stats"][시전자]["atk"]`를 읽어 일치한다.
  **일반 규칙 → `GAMEPLAY.md §값 산정` 승격 완료.**
- Q3. **`피날레 4·5` 30초 지속 vs 쿨 40초**
  **A: 실전에서는 보통 버스트 쿨타임 감소 니케를 함께 편성하므로 잘 끊기지 않는다.**
  → 예비 실측에서 재사용 간격 25.07s(표준)·26.0s(변형 2)로 실제로 끊기지 않음을 확인.
- Q4. **스킬2 실행 순서**
  **A: 최대 장탄 증가 반영 후 충전된다.** → 충전량·상한 모두 증가된 최대치 기준.
  **일반 규칙 → `GAMEPLAY.md §효과 실행 순서` 승격 완료.**
- Q5. **`최대 장탄 수 5발 ▲` 만료 시점**
  **A: 만료되면 초과 잔탄은 잘린다.** 현재 계산기는 만료 시 잔탄 재평가가 없어 미구현
  → 단계 4 신규 구현. **일반 규칙 → `GAMEPLAY.md §무기 메카닉` 승격 완료.**
- Q6. **`피날레 2·3`과 `4·5`의 중첩** **A: 합산된다.**
  **일반 규칙 → `GAMEPLAY.md §값 산정` 승격 완료.**
- Q7. **명중률·저지 부위의 딜 기여**
  **A(최종, 2026-08-11): `intercept_dmg_pct`는 구현하지 않고 조건을 언제나 미달성으로 둔다.**
  (단계 3 시점의 "구현한다" 답을, DealForm 진입 방식 3안을 제시한 뒤 이 결론으로 확정)
  → `IMPL-STATUS` 상태를 ❌ → 🚫로 바꿔 "지원 계획 없음"을 명시. 파싱은 그대로 두고
  시뮬 딜 기여 0을 네거티브 항목으로 검증한다.
  `accuracy_pct`도 기존대로 — 기본 보스 `core_px = 0`에서 무발동(네거티브 유지).
- Q8. **블랑 시나리오 영향** **A: 갱신한다.** `블랑.md` 변형 1의 "클린 대조" 서술과 블랑 baseline을
  단계 4에서 함께 고친다.

## 해석 선언

| 효과 | 해석 | 근거 | 대안이었다면 |
|---|---|---|---|
| `럭키챰` stat | **`atk_caster_based_pct`** — 누아르 기본 공격력의 14.08%를 아군 `atk_flat`에 가산 | 원문 "시전자 기준 공격력 {0}% ▲", PARSING §6 매핑 | `atk_pct`였다면 아군 각자의 공격력에 곱해져 **스쿼드 총딜이 크게 과대**. 누아르의 최대 오차원 |
| `럭키챰` timing | **`passive` + `self_hp_above:70` · `duration: -1`** | timing 키워드도 쿨타임도 없는 `[지속]` 조건부 버프. 앨리스 `건강한 당근`(`passive`+`self_hp_above:80`) 선례 | `battle_start` 1회 부여였다면 조건이 깨진 뒤 다시 참이 돼도 **재부여되지 않아** 영구 소실 |
| `럭키챰` 조건 | **런타임 재평가로 유지**(상수 참으로 접지 않음) | 유저 확인 Q1 — `max_hp_only_pct`가 조건을 끌 수 있다 | 상수 참으로 뒀다면 체력 조작 버프를 가진 스쿼드에서 이 버프가 꺼지지 않아 딜 과대 |
| `래빗 트윈즈 B` stat | **`max_ammo_flat` 5 (buff, 10초)** | 원문 `최대 장탄 수 {0}발 ▲` — %가 아니라 발 단위 | `max_ammo_pct`였다면 실효 최대 장탄이 21 → 26이 아니라 21×1.05 = 22가 되어 무기별로 증가폭이 달라짐 |
| `래빗 트윈즈 B 2` | **instant `ammo_charge_pct` 39.88 · `all_allies` · 지속 없음** | 원문 블록에 지속시간 표기 없음 | buff로 뒀다면 잔탄이 아니라 stat이 되어 아무 일도 일어나지 않음 |
| 스킬2 두 항목의 배열 순서 | **`래빗 트윈즈 B` → `B 2`** (원문 블록 순서 유지) | 유저 확인 Q4, GAMEPLAY §효과 실행 순서 | 뒤집혔다면 충전이 21발 기준으로 계산되고 21에서 캡되어 **충전량 −20%**(10발 → 8발) |
| `피날레` | **`burst_damage` · `all_enemies`** (AoE 플래그 성립) | 원문 "적 전체에게 [최종 공격력 {0}% 버스트 스킬 대미지]" | `damage`였다면 `burst_dmg_pct`·`burst_dmg_aoe_pct` 버프를 못 받아 버스트 딜이 과소 |
| `피날레 2·3` target | **`allies_weapon:SG`** (본인 포함) | 원문 "샷건 소지 아군 전체에게", PARSING §5 매핑 | `allies_weapon_excl_self:SG`였다면 **누아르 본인이 빠져** 표준 스쿼드에서 대상이 0명 |
| `피날레 4·5` | **`all_allies` + condition `squad_ally_exists` · 30s** | 원문 "동일 스쿼드 아군이 있다면 아군 전체에게" | condition을 빼면 777 동료 없는 스쿼드에서도 버프가 붙어 대상·지속이 과대 |
| `피날레` 2·3 ↔ 4·5 분리 | **4개 항목으로 분리 유지**(합산·별도 만료) | 유저 확인 Q6, 지속 10s ≠ 30s | 큰 값 하나로 합쳤다면 합산분이 사라지고(−11.61/−19.36) 만료 시각도 틀어짐 |
| `intercept_dmg_pct` | **파싱만 등록 · 구현 없음(조건 영구 미달성)** | 유저 최종 결정 Q7 — 적 모델에 저지 부위가 없다 | 구현했다면 적 스펙에 `is_intercept` 같은 플래그를 새로 두고 실제 게임에 없는 딜 축을 만들게 됨 |
| `accuracy_pct` | **기존 구현 그대로**(기본 보스 `core_px=0` 무발동) | `IMPL-STATUS.md` ⚠️ 항목, 유저 확인 Q7 | 딜 항으로 추가했다면 실제 게임에 없는 딜이 생김 |

## impl/bug-fix용 검증 체크리스트

검증 결과는 `--seed 1`, 180초, 기본 스펙 기준 (단계 4 Phase D).

**신규 구현 (단계 4)**
- [x] `_effective_max_ammo()`가 `cs._full_ammo()`에 위임 → `max_ammo_flat` 포함 (Q4).
      풀버스트 시작 충전량 8발 → **10발**, 상한 20 → **25** 확인 (t=3.25 잔탄 20→25, t=15.78 13→23)
- [x] 최대 장탄 버프 만료 시 잔탄이 원래 최대치로 캡된다 (Q5) — `CharState.tick()` 재평가
- [x] 두 수정의 방향이 반대임을 회귀로 확인 — 단독 적용 시 FAIL 스쿼드가
      flat 수정 2개(스쿼드3 +0.71%, 레이드_소다 −0.14%) / 만료 캡 10개(대부분 −)로 갈린다
- [x] `블랑.md` 변형 1 서술 갱신 + 총딜 재측정(2,436M → 2,698M). **블랑은 snapshot 스쿼드에 없어
      baseline 갱신 대상이 아니다** (Q8)
- [x] `intercept_dmg_pct`는 구현하지 않음 — `IMPL-STATUS` ❌ → 🚫로 확정 (Q7)

**발동 횟수·대상 (표준 스쿼드)**
- [x] `럭키챰` 발동 4회(아군 4인 각 1회) · 만료 없음(`expires_at = inf`)
- [x] `럭키챰` 값이 누아르 **기본** 공격력 기준 — `state["base_stats"]` 경로 확인
- [x] `래빗 트윈즈 B` 60회(=15사이클×4인) · `B 2` 15회 dispatch, 아군 4인 모두 잔탄 증가 확인
      (t=15.78: 누아르 13→23 / 크라운 680→693 / test_B3 129→137)
- [x] `피날레`~`피날레 3` 각 8회 · 버스트 딜 히트 8회 (`hit_tag: burst_damage`)
- [x] `피날레 2·3` 대상 표준 **누아르 1인** / 변형 1 **누아르+드레이크 2인**(발동 16회)
- [x] 누아르 버스트 재사용 간격 **25.067s** 균일(2사이클당 1회)

**조건 분기**
- [x] 표준·변형 1: `피날레 4·5` 발동 **0회**
- [x] 변형 2: `피날레 4·5` 각 **28회**(7시전 × 4인) · 지속 30.0s
- [x] 변형 2에서 끊기지 않음 — 재사용 간격 26.0s < 30s (Q3)
- [x] 변형 2 사이클 간격 13.0s(중간 1회 12.25) · 풀버스트 14회 — 루주 7.0 프로필과 일치

**사이클**
- [x] 표준 사이클 간격 12.533s 균일 · 풀버스트 **15회** · 총딜 1,137.1M
- [x] 변형 1 사이클 구조가 표준과 동일(15회 / 누아르 8회 / 25.067s)

**네거티브**
- [x] `accuracy_pct`가 기본 보스(`core_px = 0`)에서 딜을 바꾸지 않는다
- [x] `intercept_dmg_pct`가 `_BUFFS_ZERO`·`_STAT_TO_BUFF` 어디에도 없어 `buffs`에 집계되지 않는다
      — 버프 목록에는 뜨되 DealForm 어느 항에도 들어가지 않음 (Q7)
- [x] 누아르 본인에게 `럭키챰` 외 공격력 계열 자버프 없음
- [x] `피날레 4·5`가 표준·변형 1에 등장하지 않음

**회귀** — `python -m context.snapshot` 15/25 PASS, **10 FAIL(전부 의도된 변동, 유저 승인 대기)**
- [x] 원인 분리 완료: 10건 중 8건은 만료 캡 단독, 스쿼드3·레이드_소다는 두 수정이 겹친다
- [x] `max_ammo_flat` 보유자 조사: 그레이브 · 토브 · 트리나(+누아르). 만료 캡은 `max_ammo_pct`
      시한 버프를 쓰는 캐릭터 전반에 걸리므로 영향 범위가 이보다 넓다
- [x] 유저 승인(2026-08-11) 후 `python -m context.snapshot --update` 실행 → 재검사 **25/25 PASS**.
      갱신된 baseline 10개: 스쿼드2 · 스쿼드3 · 레이드_미하라에이다 · 레이드_레드후드퀀시 ·
      레이드_소다 · 레이드_아니스서머메이든 · 레이드_라피앨리스 · 레이드_작열짬 · 지그_리타 ·
      컨트롤_에이다미하라
