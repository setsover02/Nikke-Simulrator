# 신데렐라 : 크리스탈 웨이브 동작 시나리오

> 모드: 보강 (`/char-parse` 후. `parsed_skills.json` 기준 + 표준 스쿼드 실측 반영)

## 기본 정보
- 버스트 단계: B3
- 본인 쿨다운: 40s → **2사이클에 1회 사용** (실측: t=3.2 / 28.27 / 53.33, 간격 25.07s)
- 무기 유형: MG (base max_ammo 300 → 큐브·장비 반영 실측 328, reload 2.5s, damage_coeff 5.57, core_dmg_mult 200%)
  - 조건부 **저격 모드(SR)** 무기 변경 — §모드 전환 참조
- 속성/클래스/기업: 철갑 / 화력형 / 필그림
- 작성 사이클 수: 2 (본인 사용/미사용 사이클 비대칭)
- 스킬명: 스킬1 `뷰티-풀`, 스킬2 `모드 스왑`, 스킬3 `유리 구두 풀 컨텍트 W`

---

## 효과 요약 (parsed_skills.json 기준)

| ID | 출처 | 트리거 | stat / 효과 | 대상 | 지속 |
|---|---|---|---|---|---|
| 저격 모드 | 스킬1 | `event:full_reload` + `self_state:변경 준비` | weapon_change SR (coeff 62.13 고정, charge 1.0s, full_charge 250%, max_ammo 15) | self | 없음(해제 조건까지) |
| 저격 모드 관통특화 | 스킬1 | `event:full_reload` + `self_state:변경 준비` | `pierce_enabled` | self | 영구 |
| 저격 모드 차지시간고정 | 스킬1 | `event:full_reload` + `self_state:변경 준비` | `charge_time_fixed` 1.0 | self | 영구 |
| 저격 모드 탄 소비 집계 | 스킬1 | `full_charge_hit` | `squad_ammo_consume_add` 40 | self | 즉시 |
| 뷰티-풀 | 스킬1 | `battle_start` | `atk_dmg_pct` 24.0 | self | 영구 |
| 변경 준비 | 스킬1 | `event:full_reload` | `reload_time_fixed` 3.0 | self | 6s |
| 변경 준비 해제 | 스킬1 | `last_bullet_fire` | `remove_named_buff`(변경 준비) | self | 즉시 |
| 뷰티-풀 2 | 스킬1 | `every:5s` | `damage` 900.0 | `enemies_nearest:1` | 즉시 |
| 뷰티-풀 3 | 스킬1 | `squad_ammo_consume:200` | `burst_charge_pct` 12.0 | `all_allies` | 즉시 |
| 디코이 | 스킬2 | `battle_start`, `burst_cast` | `decoy` 70.34 | self | 영구 |
| 모드 스왑 | 스킬2 | `battle_start` | `atk_pct` 29.0 | self | 영구 |
| 디스트로이 | 스킬2 | `event:저격 모드` | `part_dmg_pct` 26.21 | self | 영구 |
| 핀 포인트 해제 | 스킬2 | `event:저격 모드` | `remove_named_buff`(핀 포인트) | self | 즉시 |
| 핀 포인트 | 스킬2 | `battle_start`, `event:state_end:저격 모드` | `core_dmg_pct` 26.0 | self | 영구 |
| 디스트로이 해제 | 스킬2 | `battle_start`, `event:state_end:저격 모드` | `remove_named_buff`(디스트로이) | self | 즉시 |
| 모드 스왑 2 | 스킬2 | `full_burst_start` + `burst_casted` + `self_state:저격 모드` | `damage` 1189.66 | `all_enemies` (원문 파츠 포함) | 즉시 |
| 모드 스왑 3 | 스킬2 | `full_burst_start` + `burst_casted` + `not_self_state:저격 모드` + `core_hit` | `core_damage` 833.79 | `all_enemies` (원문 코어 활성 적) | 즉시 |
| 유리 구두 풀 컨텍트 W | 스킬3 | `burst_cast` | `atk_dmg_pct` 92.0 | self | 10s |
| 유리 구두 풀 컨텍트 W 2 | 스킬3 | `burst_cast` | `atk_pct` 65.0 | self | 10s |
| 유리 구두 풀 컨텍트 W 3 | 스킬3 | `burst_cast` | `burst_damage` 3299.97→6000.0 | `enemies_top_atk:1` | 즉시 |

수치는 스킬 레벨 10 기준. 저격 모드 무기 스펙은 **스킬 레벨 무관 고정값**.

---

## 모드 전환 메카닉 — 실측으로 확정

`변경 준비`가 게이트, 진입·해제가 동일 조건인 토글이다.

**표준 스쿼드 60초 실측** (`event:full_reload` / `last_bullet_fire` 시점의 변경 준비 활성 여부):

| t | 이벤트 | 변경 준비 활성 |
|---|---|---|
| 8.450 | `last_bullet_fire` | False (최초 탄창) |
| 9.117 | `event:full_reload` | **False** → 전환 없음. 변경 준비 부여(6s) |
| 14.833 | `last_bullet_fire` | **True** → `변경 준비 해제`가 제거 |
| 15.500 | `event:full_reload` | **False** → 전환 없음. 변경 준비 재부여 |
| 21.233 | `last_bullet_fire` | True → 제거 |
| 21.900 | `event:full_reload` | **False** |
| … | 이하 동일 패턴 반복 | |

탄창 소진 주기 ≈ **5.72s**(328발), 재장전 ≈ **0.667s**(풀버스트 재장전 버프 반영). 변경 준비 6s는 `last_bullet_fire`(부여 후 5.72s) 시점까지 **항상 살아 있어 거기서 제거**되고, 그 0.667s 뒤 도착하는 `event:full_reload`에서는 이미 없다.

→ **자연 재장전만으로는 저격 모드 전환이 구조적으로 불가능하다.** 시뮬 180초 동안 전환 0회, `저격 모드`·`디스트로이`·`모드 스왑 2` 전부 무발동. 원본 텍스트 해석과 실측이 일치한다.

전환하려면 탄이 남은 상태의 **수동 재장전**이 필요하다(변경 준비 6초 안에, 재장전 3초 고정 → 여유 있음).

---

## 검증 스쿼드

- **표준 (MG 모드 유지)**: `["리틀 머메이드", "크라운", "신데렐라 : 크리스탈 웨이브", "test_B3"]`
  - 사이클 간격 실측 **12.533s**. CW는 사이클 0·2·4…에서 버스트.
  - 전환 0회. 실전 일반 케이스이자 현 시뮬의 기본 동작.
- **변형 1 (저격 모드 진입)**: 같은 스쿼드 + `weapon_mode_swap`
  - 진입 경로가 배선됐다 — `char["weapon_mode_swap"]=True`(CLI `--mode-swap "신데렐라 : 크리스탈 웨이브"`)면
    진입 조건이 서는 순간 수동 재장전을 삽입한다. 실전의 수동컨을 재현한 스위치다.
  - 실측(seed 42): 진입 후 유지. `저격 모드 관통특화`·`차지시간고정` 각 **7회**,
    `모드 스왑 2` **7히트**, `저격 모드 탄 소비 집계` **128회**, `디스트로이` 활성 **1회**.
    CW 총딜 **890,411,990** (MG 유지 1,038,375,214 대비 −14.2%).

조건부 효과 스캔: `back_row`·`no_burstN_ally`·`allies_weapon/class/code` 패턴 없음 → 추가 스쿼드 불필요.

---

## 사이클 타임라인 (표준 스쿼드, seed 42 실측)

### 사이클 0 — 본인 B3 사용

| t | 이벤트 | 발동 효과 | 대상/지속 | 비고 |
|---|---|---|---|---|
| 0.000 | `battle_start` | `뷰티-풀`(atk_dmg 24), `모드 스왑`(atk 29), `디코이`, **`핀 포인트`(core_dmg 26)**, `디스트로이 해제`(공회전) | self / 영구 | `디스트로이` 미부여 |
| 3.200 | CW `burst_cast` | `유리 구두 풀 컨텍트 W`(atk_dmg 92), ` W 2`(atk 65), ` W 3`(burst_damage 6000), `디코이` 갱신 | self 10s / `enemies_top_atk:1` | 실측 W 3 = 34.3M/1hit |
| 3.250 | `full_burst 시작` | `모드 스왑 3`(core_damage 833.79) — `burst_casted` ✅, `not_self_state:저격 모드` ✅, **`core_hit` ❌ 현재 무발동** | `all_enemies` | §미구현 참조 |
| 5.0/10.0 | `every:5s` | `뷰티-풀 2`(damage 900) | `enemies_nearest:1` | 180초 35회 |
| 8.450 | `last_bullet_fire` | `변경 준비 해제`(대상 없음 — 아직 미부여) | self | |
| 9.117 | `event:full_reload` | `변경 준비`(reload 3s 고정, 6s) 부여. 전환 판정 **실패** | self / 6s | |
| 13.250 | `full_burst 종료` | `유리 구두` 버프 만료(13.2) | self | |
| 14.833 | `last_bullet_fire` | `변경 준비 해제` → 제거 | self | |

### 사이클 1 — 본인 B3 미사용 (`test_B3`)

| t | 이벤트 | 발동 효과 | 대상/지속 | 비고 |
|---|---|---|---|---|
| 15.500 | `event:full_reload` | `변경 준비` 재부여. 전환 판정 실패 | self / 6s | 이하 6.4s 주기 반복 |
| 15.783 | `full_burst 시작` (test_B3) | **`모드 스왑 2`·`모드 스왑 3` 모두 미발동** — `burst_casted` 불충족 | — | 본인 미사용 사이클의 핵심 차이 |
| 15.783 | (더미 `burst_cast`) | CW 스킬3 미발동. `디코이` 갱신도 없음 | — | `디코이`는 영구라 실질 차이 없음 |
| 20.0/25.0 | `every:5s` | `뷰티-풀 2` | `enemies_nearest:1` | 버스트·모드 무관하게 계속 |
| 21.233 / 21.900 | `last_bullet_fire` / `full_reload` | 변경 준비 제거 → 재부여 | self | |
| 25.783 | `full_burst 종료` | — | | |
| 28.267 | 사이클 2 CW `burst_cast` | 사이클 0 반복 | | |

### 변형 1 — 저격 모드 (`weapon_mode_swap` 진입, 실측)

| t | 이벤트 | 발동 효과 | 비고 |
|---|---|---|---|
| T | 강제 재장전 → `event:full_reload` + `변경 준비` 활성 | `저격 모드`(SR) + `저격 모드 관통특화` + `저격 모드 차지시간고정` | 전환 판정이 변경 준비 재부여보다 **먼저** |
| T | `event:저격 모드` | `디스트로이`(part_dmg 26.21) 부여, `핀 포인트 해제` | 두 버프 동시 활성 금지 |
| T~ | SR 사격 | 차지 1s 고정, coeff 62.13 / 풀차지 250%, 관통. 장탄 15 | 발당 `저격 모드 탄 소비 집계` +40 (실제 장탄 −1) |
| T+~15s | 15발 소진 → `last_bullet_fire` → 재장전 → `full_reload` | 변경 준비 제거된 상태 → **전환 없음**, 저격 모드 유지 | |
| 본인 버스트 사이클 `full_burst_start` | `모드 스왑 2`(damage 1189.66, 파츠 포함) | `모드 스왑 3`은 미발동 | |

---

## 스킬 간 상호작용

- **`모드 스왑 2` / `모드 스왑 3`은 무기 모드에 따라 배타적으로 갈린다**
  - 경로: `저격 모드` weapon_change 활성 여부 → `full_burst_start` 시점 `self_state` / `not_self_state` 평가 → 둘 중 하나만 발동
  - 시점: **풀버스트 진입 시 1회 평가**. 그 순간의 모드가 결정적
  - 위험: 전환이 풀버스트 경계 프레임에서 일어나면 딜이 뒤집힌다. 또한 현재 구현에서는 `self_state:저격 모드`가 **영구 거짓**(weapon_change가 `_active` 미등록)이라 `모드 스왑 2`가 도달 불가능하다

- **`핀 포인트`(core_dmg_pct) → `모드 스왑 3`(core_damage) 스케일링**
  - 경로: `핀 포인트`(battle_start, 영구) → `모드 스왑 3` 계산 시 코어 배율에 합산
  - 이게 이 캐릭터 MG 모드 설계의 핵심 — 코어 대미지 버프와 코어 판정 딜이 짝이다. `core_damage`가 코어 배율을 못 태우면 **무기 200% + 핀 포인트 26%가 통째로 빠진다**
  - 대칭으로 `디스트로이`(part_dmg_pct) → `모드 스왑 2`(파츠 포함 딜)가 저격 모드 쪽 짝

- **`유리 구두 풀 컨텍트 W`/` W 2` → `모드 스왑 3` (실행 순서)**
  - 경로: `burst_cast`(t=3.200)에서 atk_dmg 92 / atk 65 부여 → `full_burst_start`(t=3.250)에서 `모드 스왑 3` 계산
  - 실측상 burst_cast가 full_burst_start보다 **0.05s 먼저** 발생 → 버프가 이미 활성인 상태로 계산된다. 순서 역전 시 딜이 크게 떨어짐

- **`변경 준비` 부여와 모드 전환 판정이 같은 `event:full_reload` (실행 순서)**
  - 판정 먼저면: 이전 변경 준비가 살아있을 때만 전환 (텍스트대로 — 수동 재장전 필요)
  - 부여 먼저면: 매 풀리로드마다 전환 (`마지막 탄환 공격 시 제거` 문구가 무의미해짐)
  - → **판정 먼저**로 확정. impl에서 명시적으로 보장할 것

- **`저격 모드 탄 소비 집계`(+40/발) → 아군 `squad_ammo_consume:N` 소비자**
  - 이 카운터는 **CW 본인만의 것이 아니다.** 리틀 머메이드 `거품 난사`(`squad_ammo_consume:500`, `sequential_damage:10`), 일레그 : 붐 앤 쇼크 `고스트 버스터 2`(`squad_ammo_consume:100`, `damage`), 그리고 CW 자신의 `뷰티-풀 3`(200발)이 모두 이 카운터를 읽는다 → **스쿼드 전체 DPS에 직결**
  - 실측(표준 스쿼드 180초): 스쿼드 총 소비 24,030발 → 리틀 머메이드 `거품 난사` 48회 × 10hit = **480 hit / 158.5M**
  - 모드별 CW 기여: MG 9,257발 vs 저격 모드 ≈150발 × 40 = 6,000 카운트 → **저격 모드로 가면 스쿼드 탄 카운터 기여가 오히려 줄어든다**. 저격 모드 평가 시 반드시 함께 볼 것
  - 벨벳은 `gauge_consume_as_ammo`(✅)로 같은 카운터를 올린다 — 선례이자 구현 참고

- **`디스트로이` ↔ `핀 포인트` 상호 배타**
  - 각 부여 clause에 상대의 `remove_named_buff`가 붙어 있다. 해제 누락 시 파츠·코어 대미지 이중 계상 → 네거티브 검증 필수

- **`디코이`는 이 캐릭터에서 하위 소비자가 없다**
  - 원본 신데렐라의 `아름다움`(디코이 조건 스택)에 해당하는 효과 없음. `decoy` stat 자체도 ❌. 본인 스킬 중 `self_state:디코이`를 읽는 것도 없음

---

## 유저 확인 사항

- Q1: 모드 전환이 수동 재장전에 의존하는데 시뮬에서 어떻게 모델링하나?
  - A: **실전은 MG 고정이 일반적. 단 수동컨으로 모드 전환하기도 함. 둘 다 염두하고 모델링.**

- Q2: 저격 모드 `최대 장탄 15발` vs `풀 차지 공격 시 탄환 소모 40발` 모순 해석?
  - A: **실제로는 1발씩 소모.** "아군이 소비한 탄의 총합"을 따지는 스킬에 대해서만 40발로 집계.
  - → 독립 instant `squad_ammo_consume_add`(fixed_value 40, `full_charge_hit`)로 분리. 기본 MG 모드는 `full_charge_hit`가 없어 자연 게이팅.
  - → **일반 규칙 승격 완료**: GAMEPLAY §무기 메카닉 `탄환 소모 N발` 표기 항목.

- Q3: 파츠(part) 관련 효과를 어떻게 처리하나?
  - A: **파츠 모델이 도입될 예정이고, 파츠 유무를 구분하는 로직이 있을 것.**
  - → `part_dmg_pct` 그대로 파싱. 현재 `is_part=True` 세팅 경로가 없어 `디스트로이` 딜 기여 0인 상태임을 인지하고 진행.

- Q4: 저격 모드 수치가 스킬 레벨 placeholder 없이 고정값?
  - A: **고정값으로 파싱.**

- Q5: `core_hit` condition이 발동하지 않는 이유는?
  - A: **`core_px`가 1 이상이면 코어 있음, 0이면 없음 판정이라 그럴 것.** 기존에는 기본공격에만 코어 판정이 있었고 명중률 기반 계산이라 괜찮았지만, **이번이 스킬에 코어 판정이 붙는 첫 사례라 다른 방식이 필요할 수 있다.**
  - → 현 코드는 `state["enemy"]["has_core"]`를 보는데 이 키를 세팅하는 경로가 없어 항상 거짓. `core_px >= 1` 기준으로 고쳐야 하고, 스킬 코어 딜은 확률 판정이 아니라 **확정 코어 명중**으로 다뤄야 한다.

- Q6: DPS 관련 여부를 임의 판단하지 말 것 (유저 지적)
  - A: `squad_ammo_consume` 계열을 "게이지 보류라 무효"로 넘겼던 것은 오판. **리틀 머메이드 `거품 난사`·일레그 `고스트 버스터 2`가 이 카운터로 직접 딜을 넣는다.** 벨벳도 탄환 소모 기믹 보유(`gauge_consume_as_ammo`로 이미 파싱됨).
  - → 실측 재확인 결과 `squad_ammo_consume:N` timing은 **구현되어 있다**(IMPL-STATUS의 ❌가 낡음).

---

## 해석 선언

| 효과 | 해석 | 근거 | 대안 해석이었다면 |
|---|---|---|---|
| `저격 모드` 진입 판정 | `event:full_reload` 시 **판정 → 부여** 순서. 이전 변경 준비가 살아있을 때만 전환 | `[마지막 탄환 공격 시 제거]`가 존재하는 이유는 자연 재장전 경로를 막기 위함 | 부여가 먼저면 매 풀리로드 토글 → 저격/MG가 6.4s마다 교대. 딜 구조가 완전히 달라짐 |
| `저격 모드` 해제 | 진입과 동일 조건의 토글. 지속시간 없음(`duration` 미기입 → `expires=inf`) | `해제 조건 : 변경 준비 상태에서 최대 장탄 재장전 완료` | 지속시간형이면 저격 모드가 N초 후 자동 복귀 → 핀 포인트/디스트로이가 주기적으로 교대 |
| `모드 스왑 3` | `core_damage` 신규 stat. 무기 `core_dmg_mult`(200%)와 `core_dmg_pct` 버프가 모두 실리는 **확정 코어 명중** 스킬 딜 | `코어 명중 대미지` 명시 + 짝인 `핀 포인트`가 `core_dmg_pct` | 일반 스킬 딜이면 코어 배율 전부 누락 → **현재 구현이 정확히 이 상태**(실측 75.9M, 코어 배율 미적용) |
| `모드 스왑 2/3` 분기 | `self_state:저격 모드` / `not_self_state:저격 모드` 배타 조건 | `[모드 적용 별 효과] [해당되는 효과만 적용]` | 둘 다 발동하면 풀버스트 딜 2배 |
| `저격 모드 탄 소비 집계` | 실제 장탄 −1, 스쿼드 카운터 +40 (분리) | 유저 Q2 답변 | 장탄도 40 소모면 15발 탄창에서 1발도 못 쏨 |
| 저격 모드 무기 수치 | 스킬 레벨 무관 고정 (`damage_coeff: 62.13` float) | template에 `{N}` 없음 + 유저 Q4 | 레벨 스케일링이면 만렙 딜이 달라짐 (츠바이·스노우 화이트는 스케일링) |
| `변경 준비` `reload_time_fixed` | 재장전 시간을 3초로 **절대 고정** (기본 2.5s보다 느려짐) | `재장전 시간 3초로 고정` | `reload_speed_pct` 계열 %감소로 보면 반대로 빨라짐 |

---

## impl/bug-fix용 검증 체크리스트

### 표준 스쿼드 (MG 모드) — 현행 동작 회귀
- [ ] `t=0` 활성: `뷰티-풀`(atk_dmg 24), `모드 스왑`(atk 29), `디코이`, `핀 포인트`(core_dmg 26). **`디스트로이` 비활성**
- [ ] 사이클 간격 **12.533s**, CW 버스트 **2사이클에 1회** (t=3.2 / 28.27 / 53.33)
- [ ] `뷰티-풀 2`: 180초 **35회** (`every:5s`)
- [ ] `유리 구두 풀 컨텍트 W 3`: 180초 **8회** (CW burst 사이클마다 1회)
- [ ] `변경 준비`: 매 `event:full_reload`마다 부여, `last_bullet_fire`에서 제거. 60초 구간 실측 **8회** 부여 (t=9.117 / 15.500 / 21.900 / 28.283 / 34.667 / 41.050 / 47.433 / 55.633)
- [ ] **`저격 모드` 전환 0회** — 자연 재장전으로는 발동 불가 (구조적)
- [ ] 발동 금지: `모드 스왑 2`, `디스트로이`, `저격 모드 관통특화`, `저격 모드 차지시간고정`, `저격 모드 탄 소비 집계`
- [ ] 발동 금지: 본인 미사용 사이클(test_B3 버스트)의 `모드 스왑 2/3` — `burst_casted` 불충족

### 배선 완료 — 스위치를 켜고 보는 검증

이 절은 원래 "미구현 → 구현 후 검증"이었다. **아래 항목은 전부 배선이 끝났다** —
기본 설정에서 안 보이는 건 미구현이라서가 아니라 **적 설정이 그것을 켜지 않기 때문**이다
(기본 적은 코어 없음·파츠 없음, 진입 스위치 꺼짐). 구현 상태의 정본은 `IMPL-STATUS.md`다.

켜는 법 세 가지 — 아래 검증은 전부 표준 스쿼드 · seed 42 기준이다.

| 축 | 스위치 | CLI |
|---|---|---|
| 저격 모드 진입 | `char["weapon_mode_swap"]=True` | `--mode-swap "신데렐라 : 크리스탈 웨이브"` |
| 코어 | `enemy["core_px"] > 0` | `--core-px 60` |
| 파츠 | `enemy["has_parts"]=True` | `--has-parts` |

- [ ] **`core_damage` + `core_hit`** (`모드 스왑 3`): `core_px=60` · MG 유지에서 **8히트 159,471,128**.
      무기 `core_dmg_mult`(200%) + `핀 포인트` `core_dmg_pct`(26%)가 실린 값이다.
      `core_hit`은 `core_px >= 1`로 판정하므로 **기본 적(코어 없음)에서 0회 발동이 정상이다** —
      예전에 적어둔 "0회 = 버그"가 아니다. 리버렐리오 `차분한 수심 2`도 같은 판정을 쓴다
- [ ] **스킬 코어 딜 판정**: 기본공격의 확률 코어히트와 달리 **확정 코어 명중**(`is_core_damage`).
      `모드 스왑 3`이 그 첫 사례이고 지금은 그렇게 계산된다
- [ ] **`squad_ammo_consume_as`** (`저격 모드 탄 소비 집계`): 저격 모드에서 **128회**.
      표기는 발당 40발이지만 발사가 이미 1발을 계상하므로 핸들러는 39발만 더 쏜다(총 40).
      실제 장탄은 −1. 소비자인 리틀 머메이드 `거품 난사`(500발)·일레그 `고스트 버스터 2`(100발)
      발동 횟수로 교차 검증한다. **예전 문서의 `squad_ammo_consume_add`는 없는 키다**
- [ ] **`reload_time_fixed`** (`변경 준비`): 재장전 3.0s 절대 고정, `reload_speed_pct` 무시.
      풀버스트 재장전 버프가 있어도 2.5s로 줄지 않는지 확인
- [ ] **weapon_change 이벤트 경로**: 진입 시 `event:저격 모드`를 스쿼드 전체에 브로드캐스트,
      종료 시 `event:state_end:저격 모드`. 둘 다 발생한다
- [ ] **`self_state:저격 모드` / `not_self_state:저격 모드`**: `_has_self_state()`가 `_active`뿐
      아니라 `state["weapon_change"]`도 보므로 모드 자체를 가리키는 조건이 성립한다.
      `모드 스왑 2`(저격)와 `모드 스왑 3`(비저격)이 이걸로 갈린다
- [ ] **모드 전환 토글 해제**: 효과에 `toggle: true`가 있고, 활성 중 같은 조건이 다시 서면
      `end_weapon_change()`로 원복한다
- [ ] **`part_dmg_pct` + 파츠 보스** (`디스트로이`): `--has-parts` + 저격 모드에서
      `모드 스왑 2` **7히트 113,884,399**, CW 총딜 890,411,990 → **900,409,103** (+약 1,000만).
      `is_part`는 원문이 파츠를 명시한 효과(`hits_parts: true`)에만 붙으며 이 캐릭터의
      `모드 스왑 2`가 유일한 보유자다. 레이븐 `급소 공략`·스노우 화이트 : 헤비암즈
      `어나더 화이트 파츠대미지`는 짝이 되는 `hits_parts` 효과가 없어 **아직 무효**다
- [ ] **코어·파츠 두 축을 같이 볼 것**: 핀 포인트(코어)→`모드 스왑 3`, 디스트로이(파츠)→`모드 스왑 2`가
      대칭이다. 한쪽 축만 켜고 비교하면 모드 우열이 성립하지 않는다

### 남은 미구현
- [ ] `burst_charge_pct`(`뷰티-풀 3`)는 버스트 게이지 모델 보류(🚫) 방침에 따름 — 이 캐릭터 고유 판단 아님

### 상호작용
- [ ] `burst_cast`(t=3.200) → `full_burst_start`(t=3.250) 순서 유지 → `모드 스왑 3` 계산 시 `유리 구두` 버프 활성
- [ ] `event:full_reload`에서 **전환 판정이 변경 준비 부여보다 먼저**
- [ ] `디스트로이`·`핀 포인트` 동시 활성 0회 (네거티브)
- [ ] 저격 모드 진입 시 CW의 스쿼드 탄 카운터 기여가 **감소**함을 확인 (MG 9,257발 → 저격 ~6,000 카운트). 리틀 머메이드 `거품 난사` 횟수 하락으로 나타남
